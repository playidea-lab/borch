# nanotorch — 어디까지 갈 것인가

"PyTorch 를 재현한다"는 목표는 측정할 수 없다. 대신 **적합성(conformance)** 으로 바꾼다.

---

## 먼저, 재현이 왜 목표가 될 수 없는가

### 분모가 1,800이다

| | 개수 |
|---|---|
| `torch.*` | 1,025 |
| `torch.nn.*` | 182 |
| `torch.Tensor` 메서드 | 604 |
| ATen 커널 (순·역방향 각각) | 863 |

우리가 실제로 쓰는 것은 **95개** — 5% 다. 나머지 95% 는 아무도 안 쓰는데
틀릴 수는 있는 표면이다.

### 비트 동등은 도달할 수 없다

```
float32 10만 개 합산
  numpy  -90.82513427734375
  torch  -90.82499694824219
  차이    1.373e-04          ← float32 eps 의 1,000배
```

합산 **순서**만으로 이만큼 벌어진다. 맞추려면 torch 의 reduction 커널 순서를 재현해야 하고,
그 순서는 SIMD 폭과 스레드 수에 따라 또 달라진다. 쫓을 수 있는 목표가 아니다.

### 완전 재현은 이 프로젝트의 원칙을 뒤집는다

nanotorch 의 원칙은 **"없는 기능이 틀린 답보다 낫다"** 이다.
재현을 쫓으면 거절 목록이 줄고 **조용히 틀릴 수 있는 표면이 커진다.**
검증이 비례해서 자라지 않으면, 커버리지가 늘수록 위험해진다.

---

## 그래서 목표는 이것이다

> **커리큘럼이 쓰는 범위 안에서, 진짜 torch 와 같은 값·같은 오류·같은 표현.**

### 충실도 등급

| 등급 | 뜻 | 목표 | 지금 |
|---|---|---|---|
| **T1 값** | `allclose(1e-5)` 로 값·모양·기울기가 같음 | 지원 범위 **100%** | **100%** (132/132) |
| **T2 오류** | 같은 조건에서 같은 종류의 예외 | 주요 오류 | 일부 |
| **T3 표현** | `print(t)` · `repr` 이 같음 | 흔한 것 | 없음 |
| **T4 비트** | 동일 비트 | **비목표** | — |

T4 를 비목표로 명시하는 것이 이 문서의 핵심이다. 적어두지 않으면 언젠가 누가 쫓는다.

### 어떻게 재는가

두 겹이다.

- `tests/test_diff.py` — 손으로 쓴 76개. 특정 함정을 겨냥한다
- `tests/conformance.py` — **표에서 생성**한 132개. 모양·dtype·인자를 곱해 뽑는다

두 번째가 확장 경로다. 연산을 표에 한 줄 더하면 케이스 수십 개가 늘어난다.

```bash
uv run --with numpy --with torch python tests/conformance.py     # 점수
uv run --with pytest --with numpy --with torch pytest tests/     # 전부
```

---

## 순서

### 1. T2 — 오류를 진짜와 맞춘다 · **다음**

모양이 안 맞을 때 나오는 메시지가 진짜와 다르면 **학습자가 검색해도 답을 못 찾는다.**
틀린 값을 주는 것과 같은 종류의 거짓말이다.

- 모양 불일치 · dtype 불일치 · `backward()` 두 번 · 그래프 없는 텐서
- 예외 **종류**를 맞추고, 메시지는 핵심 어구를 포함시킨다

### 2. T3 — `repr` 을 맞춘다

학습자가 가장 많이 하는 일이 `print(tensor)` 다. 지금은 모양이 다르다.

### 3. dtype 승격표를 전부 덮는다

첫 적합성 실행이 잡은 것이 이것이었다 — `int 텐서 + 파이썬 float` 가
torch 는 float32, numpy 는 float64. **이런 것이 더 있다고 봐야 한다.**
승격표를 표 기반 케이스로 전부 훑는다.

### 4. view 의미론

torch 의 `view`·`transpose` 는 **저장소를 공유한다.** 우리는 복사한다.

```python
a = torch.zeros(4); b = a.view(2, 2); b[0, 0] = 9
a          # torch: [9, 0, 0, 0]   nanotorch: [0, 0, 0, 0]
```

교육적으로 중요한 차이다(그리고 실무에서 사고가 나는 지점이다).
다만 구현 비용이 크다 — 저장소·스트라이드 개념을 들여와야 한다.
**T2·T3 보다 뒤에 둔다.**

### 5. `nn.RNN`

순환은 numpy 로 정직하게 구현된다. 느릴 뿐이고, 29장이 가르친다.

---

## 하지 않을 것

| | 왜 |
|---|---|
| **CUDA · 분산 · 혼합정밀도** | 브라우저에 없다. 흉내 내면 교훈이 사라진다 |
| **사전학습 가중치** | 받아오는 것 자체가 배울 점이다 |
| **JIT · `torch.compile`** | 범위 밖 |
| **속도** | 아래 참고 |

### 속도를 목표로 삼지 않는 이유

빠르게 만들려면 Rust/C++ 로 다시 써서 wasm 으로 컴파일해야 한다.
그 순간 **읽을 수 있는 교육용 구현**이라는 성질을 잃는다 — 그게 이 프로젝트의 전부인데.

그리고 그 자리에는 이미 ONNX Runtime Web 과 WebGPU 가 있다(추론 전용).
브라우저에서 진짜로 학습을 돌려야 한다면, 답은 nanotorch 가 아니라 원격 장비다.

**둘 다는 못 가진다.** 읽히는 구현이거나 빠른 런타임이거나.
nanotorch 는 전자다.
