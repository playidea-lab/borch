__pycache__/
*.pyc
.pytest_cache/
.coverage
dist/

# 골든은 생성물이다 — 1단계(네이티브 + torch)가 굳히고 2단계가 읽는다.
tests/golden.npz

# CIFAR-10 원본. 브라우저는 CORS 때문에 직접 못 받으므로 로컬로 받아 서빙한다.
cifar-10-*
cifar-batch*.bin
cifar-fake.bin
