__pycache__/
*.pyc
.pytest_cache/
.coverage
dist/

# 골든은 생성물이다 — 1단계(네이티브 + torch)가 굳히고 2단계가 읽는다.
tests/golden.npz

# CIFAR-10 원본. 브라우저는 CORS 때문에 직접 못 받으므로 로컬로 받아 서빙한다.
cifar-10-*
cifar-batch*.bin
cifar-fake.bin

# 바깥에서 받아온 것 — 저장소에는 **해시만** 둔다(tests/browser/assets.lock).
# 28MB 이고, 그중 numpy 하나가 12MB 다. 이 프로젝트가 파는 것은 17KB 짜리 휠이다.
vendor/
