{
 "autograd/Node": {
  "en": "A node in the graph. Tensor implements it.",
  "src": "b4b433a2f73f"
 },
 "autograd/Node.backwardFn": {
  "en": "Takes one output gradient and returns **as many as there are parents.**\n\nA parent that gets nothing is `null`. `null` and \"filled with zeros\" are different —\nstep functions (`sign`, `floor`) **do pass zero through.** The sister project wrote\nthis once as a refusal, and a loss with a step in it ran under torch while stopping\nonly for us.",
  "src": "ae6a15c09907"
 },
 "autograd/Node.freed": {
  "en": "Whether backward has already passed through this node.\n\ntorch releases the graph once gradient has flowed, because the intermediate values\nare holding memory. A second call is a refusal, not a recomputation.",
  "src": "c5cfe96fb878"
 },
 "autograd/Node.grad": {
  "en": "Accumulates on leaves only, as in torch.",
  "src": "94ee184b0bdc"
 },
 "autograd/Node.gradName": {
  "en": "torch's `grad_fn` name. Error messages and the golden `repr::` cases use it.",
  "src": "c2ab67395eff"
 },
 "autograd/Node.parents": {
  "en": "The inputs this value was made from. Empty at a leaf.",
  "src": "40e878c0a481"
 },
 "autograd/Node.requiresGrad": {
  "en": "Whether it receives gradient.",
  "src": "498c3951c7b3"
 },
 "autograd/backward": {
  "en": "Backpropagation. Seeds at `root` and accumulates into the leaves' `grad`.",
  "src": "1eec993f0865"
 },
 "autograd/gradMode": {
  "en": "Keeps the switch that turns gradient on and off **inside an object.**\n\nIt must not be a module-level variable. On the Python side, splitting a single file\ninto eight broke `no_grad` quietly for exactly this reason — each module got its own\ncopy, and all 374 golden cases passed straight through it. Keeping the value in one\nplace stops that from happening.",
  "src": "de9ec48f5964"
 },
 "autograd/noGrad": {
  "en": "`torch.no_grad()`. Restores even if an exception is thrown.",
  "src": "2dd34b1b1efc"
 },
 "data/": {
  "en": "Datasets and loaders — where `torch.utils.data` goes.\n\n## Without this, the story is half untrue\n\nThis repository says \"train in the browser\", and with this empty that training ends\nat **slicing batches by hand.** The accuracy runner inside the repository was in fact\nwriting its own sampling and shuffling — and every user would end up writing the\nsame thing again.\n\n## What was not built\n\n**There is no `sampler` option.** The name exists in torch, but there is nothing\nbehind it here yet. Putting the name down alone repeats what happened with\n`paramGroups` — torch's shape with nothing inside, a place where what the user passes\nis quietly ignored. What is not there is left absent.\n\nThere is no `num_workers` either. Spawning a worker does not carry the GPU handle\nacross to it.\n\n## Two places it parts from torch\n\n**Batches are GPU tensors, so they have to be received inside `scope()`.** The loader\ncannot wrap them for you — the whole point is that the tensor leaves the scope. Leave\nit unwrapped and every batch an epoch makes stays alive.\n\n```ts\nfor (const [x, y] of loader) {\n  await scope(async () => {\n    opt.zeroGrad();\n    const loss = crit.call(model.call(x), y);\n    loss.backward();\n    opt.step();\n  });\n}\n```\n\n`x` and `y` were made **outside** the scope, so they need no keep-alive mark. What\nthe scope releases are the intermediates made inside it.\n\n**Shuffling follows `manualSeed`.** torch gives the DataLoader its own generator; here\none host stream is used (`random.ts`) — one seed rewinds layer initialisation,\ndropout, the tensor factories and now batch order too. The choice was to not add\nanother door.",
  "src": "a18136bc5584"
 },
 "data/ConcatDataset": {
  "en": "Several datasets joined into one. Where `torch.utils.data.ConcatDataset` goes.",
  "src": "8e035efbc780"
 },
 "data/DataLoader": {
  "en": "Hands out batches. Where `torch.utils.data.DataLoader` goes.\n\n**It is a synchronous iterator.** Making a batch is a GPU operation and borch's\nforward is synchronous, so `for (const [x, y] of loader)` works as written — only\nreading values is asynchronous.\n\n`length` is the number of **batches**, not samples, as in torch.\n\n## Batches have to be received inside `scope()`\n\nIt is written at the head of the file too, and it is written again here — this is\n**the place a loader's user hits on their first loop**, and reading one spot of the\ndocumentation before writing code is the normal case.\n\n```ts\nfor (const [x, y] of loader) {\n  await scope(async () => {          // intermediates made in here are released on the way out\n    opt.zeroGrad();\n    const loss = crit.call(model.call(x), y);\n    loss.backward();\n    opt.step();\n  });\n}\n```\n\nThe loader cannot wrap it for you. The point is that the batch leaves the scope —\nwrap it and `x` and `y` are released before they are ever used. Leave it unwrapped\nand every intermediate an epoch makes stays, and the device fills within a few\nepochs.",
  "src": "3c17a2a8880d"
 },
 "data/Dataset": {
  "en": "Something that can be fetched by index. Where `torch.utils.data.Dataset` goes.\n\n`gather` **is optional.** With it, the loader pulls a batch in one go; without it, it\nfetches one at a time and stacks. The difference is large for a dataset holding\ntensors — at batch 32 with two tensors, GPU operations drop from 66 to 2.",
  "src": "1a26b4323c0d"
 },
 "data/LoaderOptions.dropLast": {
  "en": "Drops the last batch if it is short. Layers that need a fixed batch size use it.",
  "src": "4cc553c3d1b0"
 },
 "data/LoaderOptions.shuffle": {
  "en": "Reshuffles every epoch, as in torch.",
  "src": "ad9acc44e322"
 },
 "data/Subset": {
  "en": "Sees only part of the original. `randomSplit` produces one.",
  "src": "18ea40957d71"
 },
 "data/Subset.gather": {
  "en": "**Translates the indices and passes them to the original.** Without this, every\ndataset that has been through `randomSplit` falls onto the slow path — and splitting\ntrain from validation is an ordinary thing to do.",
  "src": "c10cb2aab596"
 },
 "data/TensorDataset": {
  "en": "A few tensors whose first axis is the sample axis. Where `torch.utils.data.TensorDataset` goes.",
  "src": "16dbf4da6413"
 },
 "data/randomSplit": {
  "en": "Splits without overlap. Where `torch.utils.data.random_split` goes.\n\n**The parts must sum to the whole.** torch stops there too — a leftover sample\nquietly discarded makes the train/validation ratio differ from the one written down,\nand nobody sees it.\n\nShuffling follows `manualSeed`. The same seed gives the same split.",
  "src": "a467b4212d8c"
 },
 "device/Availability": {
  "en": "Whether WebGPU can be used. **It answers with a value, not an exception.**\n\n`why` is what makes it worth having — `no-api` (the browser is too old, or this is\nnot a secure context) and `no-adapter` (driver blocklist, virtual machine, headless\nwith no GPU) leave the user with entirely different things to do, and folding them\ninto one exception erases that split.",
  "src": "4c976bb73d4f"
 },
 "device/Device.adapterFeatures": {
  "en": "Optional features the adapter offers. **`timestamp-query` has to be here for per-kernel timing.**\n\nA wall clock can only measure the whole step, and then there is no way to ask which of\n429 dispatches is expensive — which is exactly where this got stuck.",
  "src": "09c5fb2cad31"
 },
 "device/Device.adapterInfo": {
  "en": "Which adapter it attached to. A value anyone measuring performance must record alongside.",
  "src": "ef745e422fd7"
 },
 "device/Device.age": {
  "en": "This buffer's current life. A tensor compares this number at birth against its value at use.",
  "src": "50f474d73e52"
 },
 "device/Device.alive": {
  "en": "Whether it is still usable. Somewhere a long training loop looks at every step.",
  "src": "2269e9aa36dc"
 },
 "device/Device.alloc": {
  "en": "**Uploads must not come from the pool.** `writeBuffer` runs at the queue's current\nposition, whereas we stack commands and submit them later — if a dispatch not yet\nsubmitted is about to read a buffer taken from the pool, we would overwrite it.\nAllocating fresh removes the situation entirely.",
  "src": "a1449d043bab"
 },
 "device/Device.byKind": {
  "en": "Dispatches by kernel kind.\n\nA total alone does not say what to fix next. Whether 1,636 is twenty convs or five\nhundred BatchNorm assemblies calls for different work — this exists to measure that\nsplit.",
  "src": "7c31e103120c"
 },
 "device/Device.copyInto": {
  "en": "Overwrites one buffer with another. In-place operations use it.\n\nIt is a copy, not a kernel — the result is made in a new buffer and then moved back to\nthe original slot. Reading and writing the original at once leaves the threads\nunordered and the values mixed.",
  "src": "bb8fb6445e57"
 },
 "device/Device.dispatches": {
  "en": "Dispatches issued so far.\n\nStopping at \"it is slow\" leaves you with no next move. Knowing dispatches per step\nseparates whether the slow part is **the kernel itself or the number of calls** —\nthis design currently builds and submits a fresh command encoder per operation, so a\nlarge count points there.",
  "src": "8f4f6028fcb6"
 },
 "device/Device.emptyCache": {
  "en": "Empties the pool. Where `torch.cuda.empty_cache()` goes.\n\n**The pool does not shrink on its own.** With repeating shapes, as in a training loop,\nthat is right — remaking them each time is the cost. But when the shape **changes**,\nthe old shape's buffers stay forever. In a browser, where GPU memory is shared between\ntabs, that costs more than it does on a desktop.\n\nA returned buffer may still be referenced by commands not yet submitted, so the\nrelease happens **after** submitting.",
  "src": "7111624f517c"
 },
 "device/Device.endScope": {
  "en": "Closes the scope and releases what was made inside it.",
  "src": "d884f75750b1"
 },
 "device/Device.faults": {
  "en": "Validation errors so far.\n\n**Whoever is measuring has to look at this.** An invalid command buffer throws\nnothing and simply does no work, so the wall clock keeps running in that state and\nnumbers come out — something that looks like a measurement comes out.",
  "src": "36001dc45957"
 },
 "device/Device.flush": {
  "en": "Submits the stacked commands.\n\nIt has to be passed before values are read — reading the result of unsubmitted\ncommands returns the old value.",
  "src": "e74601e1d8e3"
 },
 "device/Device.keep": {
  "en": "Keeps something alive regardless of scope. Parameters and optimizer state use it.",
  "src": "ab3f8e120144"
 },
 "device/Device.lastScope": {
  "en": "The tally of the most recently closed scope. It stays here even when closed via `scope()`.\n\n**A non-zero `survived` means something accumulates every step** — in a training loop\nthat is the leak.",
  "src": "a9490799e1de"
 },
 "device/Device.lost": {
  "en": "The story, if the device was lost; otherwise `null`.\n\nThere is no counterpart in torch — a CUDA context lives with the process. In a\nbrowser another tab or the driver can reclaim our device, and no exception is raised\nwhen it happens.",
  "src": "c15d4e22e175"
 },
 "device/Device.memory": {
  "en": "The count and bytes of buffers currently held.\n\nA benchmark measuring leaks has to be able to ask this from outside. The sister\nproject's benchmark called `js.tf.memory()` directly, which ties the instrumentation\nto TF.js and makes the same benchmark unrunnable against another implementation — and\nthat is exactly why it could not be run.\n\nWhat sits in `spare` is excluded. A buffer back in the pool waiting for the next step\nis held, but it **is not leaking** — counting it reads something that is not a leak as\none.",
  "src": "86cf1750c3f1"
 },
 "device/Device.nsByKind": {
  "en": "GPU time in nanoseconds accumulated per kernel kind, when enabled.",
  "src": "1543b05f1bf3"
 },
 "device/Device.pipeline": {
  "en": "It does not swallow shader compilation errors.\n\n**A failed WGSL compile does not arrive as an exception.** `createShaderModule` simply\nreturns, and dispatching with that pipeline does nothing at all — the result buffer\nstays zero and all the screen says is \"the values differ\". A reduction kernel in this\nvery runner returned zeros that way, with no error visible anywhere. So the\ndiagnostics are pulled out deliberately.",
  "src": "1137ac2946f7"
 },
 "device/Device.pipelineCount": {
  "en": "Shaders baked so far. Tests look at it to see whether the cache works.",
  "src": "017de3cad70c"
 },
 "device/Device.pooled": {
  "en": "Buffers in the pool waiting for the next step. **What `memory` deliberately excludes.**\n\nThat one asks \"is it leaking\" and this one asks \"how much is held\". Two different\nquestions need two numbers, and the second one was missing — so **nobody could ask\nabout the real footprint.**\n\nThe pool grows when shapes change. It is split by size, so a buffer that ran at batch\n16 cannot serve batch 32 and simply stays. A benchmark running three batch sizes in\none pass leaves the first two sizes' worth sitting in the pool, and `memory` does not\ncount it.",
  "src": "cc4727dd5c01"
 },
 "device/Device.profile": {
  "en": "Runs `body` while measuring. **It always turns off afterwards — including on the way out through an exception.**\n\nTurning it on and off must not be left to the caller. While profiling, each dispatch\nopens a pass and the time inflates, and if it leaks out still on, **every measurement\nafter it comes out quietly inflated.** A benchmark measures several batches, so an\nexception in one batch makes the next batch's ms/step a profiled number rather than a\nmeasurement — and it prints on screen looking exactly the same. There should be one\ndoor, and the door should clean up.",
  "src": "8a729fe434ef"
 },
 "device/Device.profileDropped": {
  "en": "Dispatches **not measured** for want of room. Whoever calls has to report this alongside.\n\nNon-zero means `nsByKind` holds only part of the step while still reading like a total.\nNot writing down what was cut reads as \"everything was measured\", and that is one of\nthe kinds of lie this repository has been counting.",
  "src": "3ebc1986684f"
 },
 "device/Device.run": {
  "en": "One kernel. `groups` is the **workgroup count**, and the per-axis limit is rechecked\nhere — `kernels.ts` folds the grid, but a hand-called path may appear.",
  "src": "ac4e6a028976"
 },
 "device/Device.run1d": {
  "en": "Runs one-dimensional work spread over a grid. Paired with the indexing in `kernels.ts`.",
  "src": "9799e77ef202"
 },
 "device/Device.scopeDepth": {
  "en": "The depth of open scopes. Tests look at it for balance.",
  "src": "67af21c0919d"
 },
 "device/Device.submits": {
  "en": "Submissions actually issued so far. Whoever is measuring whether batching works looks here.",
  "src": "ef200bd56890"
 },
 "device/Device.sumAll": {
  "en": "A full sum. It calls the same kernel again until the partial sums come down to one.\n\n**It uses no atomics** — floating-point addition changes value when the order changes,\nand then the same seed run twice gives different training. The slower way is the one\nthat reproduces.",
  "src": "229439ea9ca7"
 },
 "device/Device.synchronize": {
  "en": "Waits until the submitted work has **actually finished.** Where `torch.cuda.synchronize()` goes.\n\n`flush()` returns having only put things on the queue — time it with that and the wall\nclock has already stopped while the GPU is still working. Until now the way this\nrepository forced completion was to read one value (`item()`), and that **mixes the\nreadback round trip into the measurement.** It is the place where \"am I measuring the\nkernel or the bus\" gets blurred, and this function is what separates them.",
  "src": "a94fad312801"
 },
 "device/DeviceKind": {
  "en": "Where a tensor is.\n\nWhere torch's `'cuda'` and `'cpu'` go. **There is no index** — WebGPU gives no way to\nenumerate adapters, so there is nothing for `'webgpu:1'` to point at. A string is\nenough.",
  "src": "0bb06e0e36d3"
 },
 "device/InitOptions": {
  "en": "How the adapter is chosen. Where torch's `CUDA_VISIBLE_DEVICES` goes.",
  "src": "25c965ab09de"
 },
 "device/InitOptions.forceFallbackAdapter": {
  "en": "Forces the software adapter. Used to exercise the fallback path itself.",
  "src": "a24872acb133"
 },
 "device/InitOptions.powerPreference": {
  "en": "It defaults to `\"high-performance\"` because this is a **library that measures.** The\nbrowser default may pick the integrated GPU on a laptop, and then the same code\ngives different numbers on the same machine — a number where you do not know what\nwas measured.",
  "src": "1875e9471ca0"
 },
 "device/isAvailable": {
  "en": "Asks only whether WebGPU can be used. Where `torch.cuda.is_available()` goes.\n\n**Unlike torch's, it is async** — obtaining an adapter is asynchronous and there is\nno way around it. If you need to know why not, use `probe()`.",
  "src": "2b9c39e11c5e"
 },
 "device/probe": {
  "en": "Asks whether it could attach, without attaching. It does not create a device.\n\n**It does not stand in for `init()`** — `requestDevice` can still refuse after this\npasses, and that still arrives as an exception from `init()`. What this function\nanswers reaches as far as \"is there an adapter\", and since most of what actually\nblocks sits before that, it is worth having on its own.",
  "src": "894cc9940595"
 },
 "dtype/DTYPES": {
  "en": "Every type name. **The place that decides whether a string arriving from outside is a\ntype** uses this — the label written in a checkpoint header is someone else's text,\nand must not be taken on trust.\n\nIt exports `BY_RANK` directly. Two copies of the list means one of them grows when a\ntype is added.",
  "src": "50a1fe7d53d3"
 },
 "dtype/DType": {
  "en": "dtypes and the promotion rules.\n\n## Storage is float32, and only that\n\n`int64` is a **label**. WGSL has no 64-bit integer, so there is no way to actually\nhold one, and holding it in a 32-bit integer makes it `int32`, not `int64`. So the\nvalues sit in float32 and the dtype is carried separately — the sister project made\nthe same choice, though its reason (TF.js's cast was broken) differs from this one,\nwhich is that the means of representation simply does not exist.\n\n**The price, written down**: integers above 2²⁴ are not exact in float32. The ranges\nused for indices and counts are far below that, but a large integer stored here\ndrifts quietly.\n\n## Why the split is by category\n\ntorch has categories — `bool < integer < float` — and only promotes **within** them.\nInheriting numpy's rules would make `float32 + int64` a float64, and then a learner\nlearns the wrong rule. There is one width per category here, so the rule comes out\nshorter still.",
  "src": "a71dd08e9e58"
 },
 "dtype/byRank": {
  "en": "Recovers a type from its rank. Used when a category has to be lifted a step, as in a reduction.",
  "src": "fceeb943f08c"
 },
 "dtype/floatsPerElement": {
  "en": "How many float32 slots one element occupies.\n\n**This is the one place in this file where `int64` behaves differently.** `int64` is\na label, so its storage stays one float32; `complex64` **really takes two** — real\nand imaginary interleaved.\n\nThat breaks `tensor.ts`'s long-standing invariant, **\"slot count = buffer length\".**\nThe new invariant is `buffer length = size × floatsPerElement(dtype)`, and only code\nthat knows it should touch a complex buffer — a kernel that does not will read the\nfirst half as reals and give a **quietly** wrong answer. The `Tensor.buffer` getter\nguards that door.\n\n**`complex128` does not arrive here.** WGSL has no `f64`, so there is no means at all\nof holding a double-precision complex — the core (numpy) stops at that name for the\nsame reason.",
  "src": "2db185ac7804"
 },
 "dtype/promote": {
  "en": "The result type of a binary operation.\n\nThere are only four rules.\n1. **If a complex is involved, the result is complex.** Division included — this is\n   read before rule 2 below.\n2. **Division is always float.** Even `int64 / int64` is float32 — torch's `/` is\n   true division, and integer division is a separate call, `//`.\n3. **Subtraction involving a bool is refused.** There is no settled answer to what\n   subtracting false from true means, so torch stops rather than invent one.\n4. Everything else promotes to the higher category.\n\nThe point is that rule 1 sits **before** rule 2. Behind it, `z / z` collapses to\nfloat32, and out comes a tensor that is half right — the real part survives and the\nimaginary part is gone, so the shape and the element count both look plausible.",
  "src": "0297a11602dc"
 },
 "dtype/scalarDType": {
  "en": "The type corresponding to a Python-side scalar. JavaScript does not distinguish\ninteger from float, so it is decided by the value.",
  "src": "8af9beebfb5e"
 },
 "einsum/": {
  "en": "`einsum` — contraction written in subscripts.\n\n## How far it goes\n\n**One or two operands**, no ellipsis (`...`), and no subscript repeated within a\nsingle operand (so no diagonal extraction). Anything else throws.\n\n**What it cannot do, it does not quietly wave through.** einsum's notation is short\nenough that an unsupported combination is easy to write, and if a plausible answer\ncomes back there is no way to find where it went wrong.\n\n## How it works\n\nThere is no new kernel. It moves axes (`permute`), folds them (`sumDim`), and drops\nto a matrix multiply where it can — all operations that already exist and that the\ngolden cases already watch. The value of doing it this way is that not one\nhand-written backward appears.",
  "src": "db013ead2c38"
 },
 "errors/IndexError": {
  "en": "An index past the end.\n\n**Not a `RuntimeError`.** torch uses Python's `IndexError` here, and the golden\ncases froze the kind of exception too, so it has to be that name. Blurring the\nkinds means code catching `except IndexError` stops catching — the kind of an\nexception is part of the API.",
  "src": "864792689ced"
 },
 "errors/LinAlgError": {
  "en": "Linear algebra with no answer to give — a singular matrix, a Cholesky that is not\npositive definite.\n\n**The name does work.** Code that can meet a singular matrix usually wraps the call\nin `except LinAlgError`, and throwing a plain `RuntimeError` walks straight past\nthat wrapper and kills the program. Same reason `IndexError` is kept separate —\n**the kind of an exception is part of the API.**",
  "src": "50630a75be3a"
 },
 "errors/NotImplementedError": {
  "en": "A combination that does not exist yet.\n\n**Refusals have kinds too.** torch raises \"this padding parity at this rank is not\nsupported\" as `NotImplementedError`, and that says something different from \"the\nvalue is wrong\" (`RuntimeError`) — the first is something that may exist one day,\nthe second is the caller being wrong.",
  "src": "1ea082eb0db4"
 },
 "errors/RuntimeError": {
  "en": "Present in Python, absent in JavaScript. The golden cases froze the **kind name**\nof the exception as well (`RuntimeError|message=True`), so the class name is part\nof the answer.",
  "src": "33e7fdaba1f9"
 },
 "errors/TORCH": {
  "en": "The wording torch itself produces. Kept verbatim so that searching for it works.",
  "src": "1532991709a7"
 },
 "errors/ValueError": {
  "en": "An argument that makes no sense on its own — two mutually exclusive options given\ntogether, or a value outside the domain.\n\n**It says something different from `RuntimeError`.** Python uses `ValueError` in\nthis position and so does torch (`FractionalMaxPool2d` stops with that kind when\ngiven both a size and a ratio). The golden cases freeze the **kind name** of the\nexception as the answer, so throwing a different name here leaves the binding\nnothing to carry across.",
  "src": "93d83f477cff"
 },
 "fft/": {
  "en": "`torch.fft` — the discrete Fourier transform, and `stft`/`istft` on top of it.\n\n**There is one kernel.** Forward, inverse, half transforms and the inverses of\nthose three all call the same shader with a different sign and scale. Split apart\nthey become six copies, and the day comes when one of them is fixed differently —\nthat is the kind of ground this repository has repeatedly lost.\n\n## It is O(n²), for all that it is called `fft`\n\nIt runs the DFT directly. Cooley–Tukey is only fast at powers of two, and other\nlengths need Bluestein on the side. **The values are the same either way**, and at\nthis project's ceiling — textbook-sized signals — the difference does not show. If\na day comes when it has to be fast, change it then, but **do not write down speed\nthat is not there today.**\n\n## Axes are not moved\n\nWherever `dim` points, no data is moved. Three numbers — `(outer, axis, inner)` —\nlocate the element and the shader reads it directly. Moving and moving back costs\ntwo more buffers, and moving complex data is an operation that does not exist yet,\nso it would have had to be built for this.\n\n## Gradients\n\nThe transform is **linear**, so the backward falls straight out of the convention.\nThe backward of a holomorphic function is `conj(f')·g`, which gives\n`grad_x[j] = Σ_k e^{+2πijk/n}·g[k]` — **an unnormalised inverse transform.** The\nhard part is not the value but **which half gets counted**:\n\n* `rfft`'s backward receives gradient only on the stored half — adding the\n  conjugate pair doubles it.\n* `irfft`'s backward counts **the edges once and the middle twice**, because the\n  conjugate pair it restored came out of the same stored slot.\n\nEither can be wrong **while the forward values stay perfectly fine**. The core\n(numpy) went through the same derivation first, and the golden cases ask about the\ntwo separately.",
  "src": "67611b57d0c5"
 },
 "fft/fftshift": {
  "en": "Zero frequency to the middle. **It shifts by `n//2`** (measured — including at odd lengths).",
  "src": "55ed8def70f5"
 },
 "fft/hfft": {
  "en": "Hermitian-symmetric complex → **real.** The conjugate relation of `irfft` (measured).",
  "src": "7a88ec90ed73"
 },
 "fft/hfftn": {
  "en": "The last axis is `hfft` and **the axes before it are `fft`.**\n\nBeing the mirror of `rfftn` it looks like it should be `ifft`, but torch uses `fft`\n(measured — both candidates were built and compared). **The shape is right either\nway**, so it does not surface unless the values are measured.",
  "src": "1699ee530203"
 },
 "fft/ifftshift": {
  "en": "The undo. **Shifting back by `n//2` does not land at odd lengths** — the same amount, the other way.",
  "src": "63c28fab7d99"
 },
 "fft/ihfft": {
  "en": "Real → **Hermitian-symmetric complex.** The conjugate of `rfft`.",
  "src": "05ae0b536595"
 },
 "fft/irfftn": {
  "en": "The inverse of `rfftn`. **`ifft` first, `irfft` last.**",
  "src": "ac10fe28b9d9"
 },
 "fft/rfftn": {
  "en": "Real input. **Only the last axis is `rfft`; the rest are `fft`** — the order decides the answer.",
  "src": "90f2fb4c1865"
 },
 "index/": {
  "en": "borch — a PyTorch-shaped tensor library running on WebGPU in the browser.\n\n```ts\nimport { init, Tensor, nn, optim, scope, keepAlive } from \"borch\";\n\nawait init();                                  // acquire the WebGPU adapter\n\nconst model = new nn.Sequential(\n  new nn.Linear(784, 128), new nn.ReLU(), new nn.Linear(128, 10));\nconst opt = new optim.SGD(model.parameters(), 0.05, 0.9);\nconst crit = new nn.CrossEntropyLoss();\n\nconst x = keepAlive(Tensor.from(pixels, [32, 784]));\nconst y = keepAlive(Tensor.from(labels, [32], { dtype: \"int64\" }));\n\nfor (let i = 0; i < steps; i++) {\n  await scope(async () => {                    // release one step's intermediates\n    opt.zeroGrad();\n    const loss = crit.call(model.call(x), y);\n    loss.backward();\n    opt.step();\n    console.log(await loss.item());\n  });\n}\n```\n\n## What this file does\n\nIt gathers the public surface **into one place.** Without it, users have to point\nat inner paths like `dist/src/tensor.js`, and then the moment we move a file their\ncode breaks. What is written here is the promise; the rest is internal business.\n\n## Where it parts from torch — written down in advance\n\n**`await init()` has to be called first.** Acquiring the WebGPU adapter is\nasynchronous and there is no way around it. Make a tensor without calling it and it\nstops there with a message. Where `torch.cuda.is_available()` goes, use\n`await isAvailable()`, and if you need to know *why* it is unavailable,\n`await probe()` separates `'no-api'` (the browser is too old) from `'no-adapter'`\n(driver blocked, headless).\n\n**`'cpu'` is a place values are held, not a device that computes.** Bring values\ndown to the host with `await t.cpu()` and back up with `t.webgpu()`. A tensor that\nhas come down can be read (`toArray`, `item`, `repr`) but cannot be computed with —\nborch has no CPU kernels. Feed one in and it stops with torch's own wording\n(\"Expected all tensors to be on the same device\"). `t.device` answers where it is.\n\n**Reading values is asynchronous.** `await t.item()`, `await t.toArray()`. It is a\ntrip back for GPU memory. Forward and backward are synchronous.\n\n**Training needs a scope open.** Intermediate buffers made inside `scope()` are\nreleased on the way out. JavaScript's garbage collector does not return GPU memory\nin time, so leaving the thousands one step makes fills the device within a few\nsteps. It is a concept torch does not have, and it sits where TF.js's `tidy` sits.\nAnything that has to survive, such as parameters, is marked with `keepAlive`.\n\n**There are two spellings — the one closer to Python's `with` is recommended.**\n\n```ts\nfor (let i = 0; i < steps; i++) {\n  using s = scope();                 // closes at the end of the block\n  opt.zeroGrad();\n  const loss = crit.call(model.call(x), y);\n  loss.backward();\n  opt.step();\n  console.log(await loss.item());\n}\n```\n\nReleasing is synchronous, so it is `using` and not `await using`. The block is left\nonly after every `await` inside it has finished, which makes the `await loss.item()`\nabove safe (measured). The callback spelling stays as well — it is shorter where you\nwant the value handed straight back.\n\n**Forgetting stops loudly.** Using a tensor carried out of a scope without being\nmarked raises at that point. For a while it did not, and back then **whatever the\nnext allocation had written over it** was read quietly (measured: `[1,2,3,4]` came\nback as `9,9,9,9`).\n\n**A model is called as `model.call(x)`.** JavaScript cannot simply call an object,\nso torch's `model(x)` cannot be carried over as written. Calling `forward` directly\ngives the same value, but `call` is the recommended one — it is where hooks would\nattach if they ever do.",
  "src": "a931af7ca0d0"
 },
 "index/isTensor": {
  "en": "Whether this is a tensor.\n\nCode wrapping this library from outside needs it — it has to treat a method's result\ndifferently depending on whether it returns a tensor, a number or a shape, and using\n`instanceof` means importing the class, which other languages cannot do. Pyodide's\nPython binding is the first user.",
  "src": "1be95b22ff71"
 },
 "index/setNull": {
  "en": "Empties one slot **to `null`.**\n\n## Why this is exposed\n\nPython cannot make JavaScript's `null`. Pyodide hands `null` to Python as `None`,\nand sending `None` back the other way produces **`undefined`** — which in JavaScript\nis a different value.\n\nSo `p.grad = None` left the slot on the other side as `undefined`, and `autograd.ts`\nasks whether it is empty with `node.grad === null`. The comparison came out false,\nit tried to accumulate gradient into something that was not there, and what blew up\nwas `Cannot read properties of undefined (reading 'add')` — a long way from the\nPython line.\n\n**Loosening the strict comparison over there is not the fix.** That weakens the TS\ninvariant and leaves the real cause, the conversion, exactly where it was. Put the\nway to make a null on the side that has nulls.",
  "src": "1659aeb686fb"
 },
 "indexing/AxisPlan": {
  "en": "How one axis is to be read — **a plan, not a value.**\n\nKeeping parsing apart from execution means this file never calls `Tensor` (no\ncycle). Ranges and ordering are all settled here, so the side that executes only\never receives numbers that already hold.",
  "src": "15125a26c1e4"
 },
 "indexing/Slice": {
  "en": "`x[start:end:step]`. Do not build one directly — use `slice()`.",
  "src": "e2374ad8cbd7"
 },
 "indexing/planAxis": {
  "en": "A single-axis index, turned into a plan.",
  "src": "a895b1e59c0c"
 },
 "indexing/slice": {
  "en": "The span to cut. An empty position runs to the end, as in Python.\n\n**A negative step is refused.** Python's `x[::-1]` reverses, but there is a `flip`\nhere that already does that job. Letting two spellings do one thing blurs which is\ncanonical, and that is a place this repository has repeatedly stayed away from.",
  "src": "19c139f04b1c"
 },
 "linalg/LU.sign": {
  "en": "The sign of the number of row swaps. The determinant's sign comes from here.",
  "src": "63e68a0d1b99"
 },
 "linalg/LuPacked": {
  "en": "What `lu_factor` produces — `L` and `U` packed into one plate, plus the pivot table.\n\n**The pivot table counts from 1.** That is the LAPACK convention and torch inherited\nit as-is. A 2×2 with no swap gives `[1, 2]`, not `[0, 1]` — count from zero and\n`luSolveFactored` gives a different answer without a sound. Matched by asking real\ntorch.",
  "src": "419a378cd18f"
 },
 "linalg/Mat": {
  "en": "A square matrix laid out row-major.",
  "src": "eea66fde901b"
 },
 "linalg/cholesky": {
  "en": "The lower-triangular Cholesky. Throws if the matrix is not symmetric positive definite — it does not quietly return NaN.",
  "src": "31d74bd05236"
 },
 "linalg/choleskyBackward": {
  "en": "Cholesky's backward.\n\nThe gradient towards `A` when `L = chol(A)`. The derivation is not short, so the\nformula is written out — with `P = Φ(Lᵀ·L̄)`, where `Φ` takes the lower triangle and\nhalves the diagonal, symmetrise `Ā = L⁻ᵀ·P·L⁻¹`. The symmetrisation is needed because\n`A` is symmetric, so its upper and lower triangles share the same degrees of\nfreedom.",
  "src": "a0a1f0d51c6c"
 },
 "linalg/completeBasis": {
  "en": "Fills `rows × k` orthonormal columns out to `rows × rows`.\n\nIt finds directions orthogonal to the existing columns by Gram–Schmidt. **With more\nthan one dimension left over the answer is not unique** — any rotation inside that\nsubspace is the same factorisation. That is why the golden cases ask in absolute\nvalue, and only measure where exactly one dimension is left.",
  "src": "6552c748bde2"
 },
 "linalg/eigh": {
  "en": "Eigenvalues and eigenvectors of a symmetric matrix — Jacobi rotations.\n\nEigenvalues come back in **ascending** order, as torch's `linalg.eigh` gives them.",
  "src": "662492736473"
 },
 "linalg/eighGap": {
  "en": "`F_ij = 1/(λⱼ − λᵢ)`, which enters `eigh`'s eigenvector backward.\n\n**Repeated eigenvalues blow it up.** torch blows up with it, so this is the same\nlimit rather than an imitation. The diagonal is a difference with itself, so it is\nzero by definition rather than a division.",
  "src": "b31a20d7dbca"
 },
 "linalg/lu": {
  "en": "LU factorisation with partial pivoting.\n\n**Pivoting is not optional.** Without it, a matrix whose first element is zero or\nvery small either blows up the division or loses the digits entirely.",
  "src": "a71a827037ba"
 },
 "linalg/luExpand": {
  "en": "Unpacks the packed plate into `P`, `L` and `U`.",
  "src": "a06f2145a649"
 },
 "linalg/luSolveFactored": {
  "en": "Solves `A x = b` using what `lu_factor` produced.",
  "src": "c036d680bbca"
 },
 "linalg/matrixExp": {
  "en": "The matrix exponential `e^A` — **scaling and squaring.**\n\nTaylor alone does not converge on large matrices. The answer for `A*5` is 4.8e+10,\nand at that point the growing terms overflow first. Bringing the 1-norm of `A/2^s`\nbelow 0.5, running the series, and then squaring `s` times gives the same answer\nsafely — `e^A = (e^{A/2^s})^{2^s}`.",
  "src": "26884bce92b3"
 },
 "linalg/matrixExpAdjointMap": {
  "en": "Freezes the backward of `e^A` **into a single matrix.**\n\nThe Fréchet derivative has this identity — the upper-right block of\n`expm([[Aᵀ, E],[0, Aᵀ]])` is the derivative in the direction `E`. It is an identity,\nnot an approximation.\n\n**That route is not available here, though.** The `Ḡ` arriving in the backward is on\nthe GPU and this file is CPU — you cannot wait on the GPU from inside a backward. So\nit uses the fact that `Ḡ` enters **linearly**: solve once for each `E_k` with a\nsingle one in one slot, build an `n²×n²` table, and the backward is one matrix\nmultiply.\n\nThe values are exact (each column is a real derivative, not a finite difference).\nThe price is `n²` calls to a `2n×2n` `expm` in the forward, which is cheap at the\nsizes being pushed here.",
  "src": "26a7ae31482e"
 },
 "linalg/matrixRank": {
  "en": "The count of non-zero singular values. What counts as zero is scaled to the largest singular value.",
  "src": "e6ee4f5beb6d"
 },
 "linalg/mirror": {
  "en": "Keeps one triangle and mirrors it into the other.\n\n**`eigh` accepts matrices that are not symmetric.** By default torch reads only the\nlower triangle and ignores the upper — `[[4,99],[1,3]]` and `[[4,1],[1,3]]` give the\nsame answer (asked of real torch). Jacobi looks at the whole matrix, so without\nmirroring first, a non-symmetric input diverges quietly here. **It is a difference\nthat never shows as long as you pass something symmetric.**",
  "src": "cfd47c07ad98"
 },
 "linalg/pinverse": {
  "en": "The Moore–Penrose pseudoinverse. Directions whose singular value is near zero are dropped.",
  "src": "25ecc555fe4a"
 },
 "linalg/qr": {
  "en": "Householder QR.\n\n**Householder, not Gram–Schmidt.** The two give the same factorisation with\ndifferent signs. LAPACK uses Householder and torch uses LAPACK, so matching the\ngolden `R` down to its signs requires the same method — the golden cases ask about\n`Q` in absolute value, but they ask about `R` as it is.",
  "src": "48979cd1f5d9"
 },
 "linalg/slogdet": {
  "en": "The sign and the log absolute value, separately. Safer than `log(det)` when the determinant is very small.",
  "src": "6ffda5c19940"
 },
 "linalg/solve": {
  "en": "Solves `A X = B`. `B` has `m` columns.",
  "src": "5ff9bba8e120"
 },
 "linalg/solveTriangular": {
  "en": "Solves **knowing** the matrix is triangular. One pass, forward or backward, and it is done.",
  "src": "adaae441eb60"
 },
 "linalg/svd": {
  "en": "Singular value decomposition. **Rectangular input is accepted.**\n\nIt is obtained from the eigendecomposition of `AᵀA` — the shortest route that holds\nfor small matrices. Singular values come back in **descending** order (as in torch).\nOn large or ill-conditioned matrices this method loses digits, but that is not the\nsize being pushed here.\n\n`u` is **reduced** — `rows × k` (`k = min(rows, cols)`). torch's default,\n`full_matrices=True`, wants `rows × rows`, so `completeBasis` fills the remaining\ncolumns. The two are kept apart because the reduced form is always unique while the\nfilled one is **not** once more than one dimension is left over.",
  "src": "144f401fbc0c"
 },
 "nn/": {
  "en": "Layers — the minimal skeleton of `nn.Module`.\n\n## What it is for\n\nUnit comparison looks at one operation at a time. Some things diverge only once\nmodules, losses and optimizers are wired together, and every defect this repository\ncaught in an integrated scenario came from exactly there. So building layers is not\na convenience — it is **seeing a place that could not be seen.**\n\n## The name is the contract\n\nA `state_dict` key is built from **the position index and the name**, as in\n`0.weight`. The golden cases put weights in by that name and take results out by\nthat name, so a divergent name means divergent wiring, not divergent values.",
  "src": "8ea9e52cb44b"
 },
 "nn/AdaptiveLogSoftmaxWithLoss": {
  "en": "Softmax for a very large vocabulary. **It makes frequent tokens cheap.**\n\nTokens are bundled by frequency; the leading bundle is produced straight from the head,\nand the trailing bundles as **the probability the head picked that bundle × the\nprobability within it**. The further back, the narrower the intermediate dimension gets,\ndivided by `divValue` — less room spent on rare tokens.\n\n**The defaults are `divValue=4` and `headBias=false`** (measured: `tests/probe_asm.py`).\nThe intermediate dimension is `inFeatures / divValue**(i+1)` floored and **can reach 0** —\ntorch makes an empty layer there and carries on, so this does not block it either.",
  "src": "51b1e186b81c"
 },
 "nn/AdaptiveLogSoftmaxWithLoss.forward": {
  "en": "**This layer takes the target too.** Trying to pass through stops here — `run(x, target)` is the place.",
  "src": "ea9146aa2361"
 },
 "nn/AdaptiveLogSoftmaxWithLoss.logProb": {
  "en": "The log probability of every token, `(N, nClasses)`.\n\nTo the probability within a bundle it **adds the log probability that the head picked\nthat bundle** — multiplication is addition in logs, which is what leaves the whole row\nsumming to 1.",
  "src": "f81f73b84a29"
 },
 "nn/AdaptiveLogSoftmaxWithLoss.run": {
  "en": "`{ output, loss }` — the log probability at the target position and the negative of its mean.",
  "src": "b78269f9df92"
 },
 "nn/BatchNormND.namedBuffers": {
  "en": "**The running statistics go out too.** Left out, it is quietly wrong in eval mode only.\n\nA field named `runningMean` has to leave under the key `running_mean`, which rules out\n`registerBuffer` (there the field name is the key). Instead **the list lives here alone** and\n`stateDict` derives from it the same way the base does — the three falling out of step has\nalready happened twice in this layer.",
  "src": "f4ee1a9c8ea0"
 },
 "nn/Bilinear": {
  "en": "Mixes two inputs **at once.** The weight has three axes, `(out, in1, in2)`.",
  "src": "095041d0c4a6"
 },
 "nn/CTCLoss.describe": {
  "en": "torch's `extra_repr` produces nothing here — hence empty.",
  "src": "e24a2c224f86"
 },
 "nn/ConvND": {
  "en": "A convolution layer independent of dimensionality. `spatial` is the number of spatial axes.\n\n`Conv1d`, `Conv2d` and `Conv3d` differ only in that number — stand up three classes and\na day comes when only one of them is fixed.",
  "src": "ddba5435a5f8"
 },
 "nn/ConvND.bias": {
  "en": "The bias may be absent — with normalisation following, the bias is absorbed as a constant term.",
  "src": "79f9879918a5"
 },
 "nn/ConvTransposeND": {
  "en": "Transposed convolution. **The weight is `(in, out, …)`** — flipped relative to `ConvND`.\n\nWith a square kernel the shape still matches when flipped, so only the values diverge.\nThe `stateDict` keys are the same, `weight` and `bias`, so putting them in by shape alone\nis quietly wrong.",
  "src": "58e87b8f594a"
 },
 "nn/CrossEntropyLoss": {
  "en": "Straight from logits and target indices. `log_softmax` and `nll_loss` joined.\n\n**There was no `reduction`** — the layer side had the same hole. Textbooks use layers more\nthan functions, so this is the more frequently touched side.",
  "src": "bc04d3abf63a"
 },
 "nn/Dropout": {
  "en": "Drops slots only while training. **In eval mode it is the identity.**\n\n`training` is held by `Module`, and `eval()` reaches down through containers to turn it\noff — break that propagation and training is fine while only inference is wrong.",
  "src": "ae4437960bec"
 },
 "nn/ELU": {
  "en": "The negative side lies down exponentially. **Without varying α it cannot be told apart from the argument-free entry in the table.**",
  "src": "29b1e6bdd670"
 },
 "nn/EmbeddingBag": {
  "en": "One row per bag. Selecting from the table **and combining** is one layer.",
  "src": "f003c011f89d"
 },
 "nn/EmbeddingBag.callOffsets": {
  "en": "Cuts a one-dimensional index run into bags with `offsets`.\n\n**Bags of differing lengths are the reason this name exists.** A two-dimensional input\nonly allows bags of equal length, and the actual uses (shopping carts, sentences) have\ndiffering lengths.",
  "src": "486bc9d59d29"
 },
 "nn/Flatten": {
  "en": "Flattens, keeping only the batch axis.",
  "src": "06d12131b528"
 },
 "nn/FractionalMaxPoolND": {
  "en": "Max pooling whose window positions are shaken by a sample.\n\n**The sample is drawn on the host.** The window positions have to be baked into the\nshader, so the values must be on the CPU, and reading GPU randomness back is\nasynchronous — `forward` has to be synchronous, so it uses the stream in `random.ts`.\n`manualSeed` catches that stream too.\n\nPass `randomSamples` and it uses those. It is where torch's `_random_samples` goes, and\nit is needed to freeze values for comparison — without it the three implementations'\ngenerators differ and the values do not match.\n\n**`outputSize` and `outputRatio` are one or the other.** Giving both, or neither, stops\n— as in torch.",
  "src": "c3c513e9b228"
 },
 "nn/GELU": {
  "en": "**`approximate` is not accepted for show** — `\"tanh\"` is a different formula and a\ndifferent value.\n\nThe maximum difference is around 1e-4, which is near this project's tolerance, and so it\nis a place where \"near enough, keep one\" almost got through. The golden cases ask about\nthe two separately.",
  "src": "1b0b24352956"
 },
 "nn/GroupNorm": {
  "en": "Normalises with the channels bundled into groups. **Used instead of BatchNorm when the batch is small.**\n\nBatchNorm uses batch statistics, so with a batch of 1–2 those statistics cannot be\ntrusted. This one bundles within a single sample, so it is independent of batch size.",
  "src": "9c7d8c430c87"
 },
 "nn/Identity": {
  "en": "Returns what it was given. Used as a placeholder in a `Sequential`.",
  "src": "33c4673aa485"
 },
 "nn/InstanceNormND": {
  "en": "Per sample and per channel. **The default is no parameters** — as in torch.\n\nIt is the opposite of `BatchNorm`, which makes it a confusing place, and flipping the\ndefault changes every `stateDict` key.",
  "src": "cfc28375ae5f"
 },
 "nn/LPPool1d": {
  "en": "p-norm pooling. **The first argument is `normType`**, not the kernel (that is torch's shape).",
  "src": "6d60019011be"
 },
 "nn/LSTMCell": {
  "en": "**Alone in returning two** — `(h, c)`. Forcing the three into one shape loses the memory cell.",
  "src": "e97b12b09240"
 },
 "nn/LayerNorm": {
  "en": "Trailing axes to mean 0, variance 1. **`normalizedShape` decides how many axes are folded.**\n\nMeasured only with a single axis (`new LayerNorm(4)`), the answer matches \"fold the last\naxis\" and the rule stays invisible. Given `[3, 4]` it folds the last two axes **as one\nblock.**\n\nThe default is with parameters — the opposite of `InstanceNorm`, which makes it a\nconfusing place, and flipping the default changes every `stateDict` key.",
  "src": "9bf8d5248cea"
 },
 "nn/LazyModule.constructor": {
  "en": "What to read off the input. Linear reads the last axis; the rest read channels.",
  "src": "5f02c1999177"
 },
 "nn/LazyModule.label": {
  "en": "The text to print before it solidifies. `describe()` uses it.",
  "src": "63ec62f8c320"
 },
 "nn/Linear": {
  "en": "`y = x·Wᵀ + b`. The weight is `(out, in)`, as in torch.",
  "src": "e6950ab44562"
 },
 "nn/Linear.bias": {
  "en": "**It may be absent.** The tail layer of `AdaptiveLogSoftmaxWithLoss` uses no bias.",
  "src": "c2efcd9f473b"
 },
 "nn/Linear.describe": {
  "en": "Exactly what Python prints.\n\n**When a lazy layer solidifies, this text is the answer** — from then on the thing is a\n`Linear`, and this is what the user sees from `print(model)`.",
  "src": "44082d113f1e"
 },
 "nn/Module": {
  "en": "One layer. It passes values through and hands out its parameters with their names.",
  "src": "6fc8af613a99"
 },
 "nn/Module.call": {
  "en": "So it can be called — the same position as torch's `model(x)`.",
  "src": "ff2cf57abddd"
 },
 "nn/Module.claim": {
  "en": "Keeps parameters alive outside a scope.\n\nBuild a layer **inside** a training scope and its weights are released when that scope\ncloses. Building outside is the correct way, but nailing it down here is better than\ngoing quietly strange when the correct way is not followed.",
  "src": "ce46ab1040de"
 },
 "nn/Module.describe": {
  "en": "The text `print(model)` prints. The same shape as torch's `__repr__`.\n\n**A child's several lines are re-indented too.** When a container holds a container (a\n`Sequential` inside a `ModuleList`), the inner one sitting flush left makes the picture\ndiffer from torch's — the kind where the values are fine and only the text is wrong, so\nonly an eye catches it.\n\nA layer that wants to print a name or its arguments overrides this.",
  "src": "a3b3e94d4432"
 },
 "nn/Module.loadStateDict": {
  "en": "Puts weights in from outside.\n\n**It moves values only; it does not swap the tensor.** Swapping makes it a different\ntensor from the one the optimizer holds, and then training runs while the parameters\ndo not move.",
  "src": "4aa8a0b43a91"
 },
 "nn/Module.namedBuffers": {
  "en": "A list differing from `namedParameters` by **exactly the buffers.**\n\nIts own come from the registered table and its children's by asking the children. A\nlayer that writes `stateDict` by hand (`BatchNormND`) has to override this too, or the\nthree fall out of step.",
  "src": "adf3d22059b8"
 },
 "nn/Module.namedChildren": {
  "en": "Gives the children **with their names.** The default is **the field name**, as in torch.\n\n```ts\nclass Net extends nn.Module {\n  fc1 = new nn.Linear(4, 8);      // → \"fc1.weight\", \"fc1.bias\"\n  fc2 = new nn.Linear(8, 2);\n  forward(x) { return this.fc2.call(this.fc1.call(x)); }\n}\n```\n\n**Arrays are not swept.** `layers = [a, b]` is not a child — torch does not register a\nPython list either, and asks for `nn.ModuleList`. Sweeping arrays leaves no way to\ntell them from \"an array that is not layers\", and without that distinction the\n`state_dict` keys change quietly.\n\nContainers that want to be addressed by position (`Sequential`, `ModuleList`) override\nhere.\n\n**The `state_dict` keys are these names.** Diverge and you cannot read anyone else's\ncheckpoint.",
  "src": "ab4cdfecd252"
 },
 "nn/Module.namedParameters": {
  "en": "Labels with the position name prefixed, as in `0.weight`.",
  "src": "9c9fc7ece03f"
 },
 "nn/Module.ownParameters": {
  "en": "The parameters this layer holds directly. Children come from `children`.\n\n**The default is to sweep its own fields.** It used to return `{}` and have each layer\noverride, and then the moment someone building a layer from outside forgets the\noverride, that parameter is absent from `parameters()` and — **with no exception** —\ntraining simply does not happen. The kind where the loss will not come down and\nnothing points at why.\n\nA comment said \"TypeScript has no place to recognise layers by sweeping properties\",\nand that was not true. `Object.entries(this)` gives the instance fields — the same\nposition torch's `__setattr__` occupies.\n\n**`requiresGrad` is the mark.** Counting every tensor in a field as a parameter would\nhave the optimizer stepping constants and masks too. The distinction torch draws with\n`nn.Parameter` is drawn here by this flag — a layer raises it with `claim()`, and that\nis what \"this is a parameter\" means.",
  "src": "be28f5a515ec"
 },
 "nn/Module.registerBuffer": {
  "en": "A value that is not trained but is saved and restored. torch's `register_buffer`.\n\n**There used to be no such thing.** The only route was `BatchNormND` writing its own\n`stateDict` by hand to carry the running statistics, which made buffers **that layer's\nspecial case.** Every model carrying masks, position tables or normalisation constants\nuses this idiom, and it was unavailable.\n\nWith `persistent=false` it drops out of `stateDict` — the place for a cache that can be\nrebuilt rather than checkpointed.",
  "src": "8f4502916cb4"
 },
 "nn/Module.training": {
  "en": "Whether it is in training mode. Layers that behave differently by mode, such as `BatchNorm`, look at it.",
  "src": "2e88eb8eab8d"
 },
 "nn/ModuleDict": {
  "en": "A named bundle of layers. The name you give becomes the `stateDict` key as-is.",
  "src": "3fb520559657"
 },
 "nn/ModuleList": {
  "en": "A list of layers. The index is the name — `layers.0.weight`.\n\nWhat differs from `Sequential` is that it **does not call them.** In what order and how\nthey are used is up to the holder; this side only makes the parameters visible. Models\nwhose layer count is not fixed use it.",
  "src": "c6ee6d58447b"
 },
 "nn/ModuleList.forward": {
  "en": "**It is not a layer you call.** Trying to pass through stops here — as in torch.",
  "src": "0d3fc56d88b2"
 },
 "nn/ModuleList.namedChildren": {
  "en": "Addressed by position index. Same reason as `Sequential` — the layers live in an array.",
  "src": "5e3396a00df1"
 },
 "nn/MultiheadAttention.causalMask": {
  "en": "A mask that only lets it look backwards. Fills the upper triangle with -inf.",
  "src": "6787e7c3d47b"
 },
 "nn/PReLU": {
  "en": "**Learns** the slope on the negative side. The only one in this family with a parameter.\n\nThe name `weight` becomes a `stateDict` key, so it has to match torch.",
  "src": "8a55caac648e"
 },
 "nn/PadNd": {
  "en": "The root of fifteen padding layers.\n\n**Three (1-, 2-, 3-dimensional) × five (reflect, replicate, zero, constant, circular)\ncome out of one machine.** All that differs is the mode name and the number of pairs, so\nwriting fifteen copies by hand scatters two genuinely different things across fifteen\nplaces.",
  "src": "c0a2842e272a"
 },
 "nn/PadNd.constructor": {
  "en": "The name to print. **It does not lean on `constructor.name`** — the moment a bundler\nshortens names, different text comes out quietly, and the golden cases only ever see our\nbuild.",
  "src": "9016731c84fb"
 },
 "nn/PadNd.describe": {
  "en": "Exactly what Python prints.\n\n**Only `ConstantPad` prints with the name attached** — the rest print only the pairs.\nReal torch does that and the golden cases froze the text, so the difference is part of\nthe answer. The value is in Python's form too, so an integer still carries a decimal\npoint (`value=7.0`).",
  "src": "f4a35ff9adbd"
 },
 "nn/PadNd.label": {
  "en": "The name to print. **It does not lean on `constructor.name`** — the moment a bundler\nshortens names, different text comes out quietly, and the golden cases only ever see our\nbuild.",
  "src": "9016731c84fb"
 },
 "nn/PairwiseDistance": {
  "en": "The distance between two paired rows. **`eps` is added to the difference** — written down on the tensor side.",
  "src": "a7c162ccea7a"
 },
 "nn/Parameter": {
  "en": "Where `nn.Parameter` goes. **It stands up a new leaf.**\n\n```ts\nclass Net extends nn.Module {\n  w = new nn.Parameter(Tensor.zeros([4]));\n  forward(x: Tensor) { return x.mul(this.w); }\n}\n```\n\n## Two places it parts from torch — measured\n\n**Storage is not shared.** torch's `nn.Parameter(t)` sees the same storage as `t`, so\nediting one changes the other; having no views, we **copy the values.** It is the same\nreason views are refused, and the Python binding made the same choice at the same place\n— two GPU implementations diverging from each other is worse than diverging from torch.\n\n**`requiresGrad` alone makes something a parameter.** torch puts only what is wrapped in\n`Parameter` into `named_parameters()` and leaves an ordinary `requires_grad=True` tensor\nout (measured). Here `ownParameters()` looks at the flag, so both come in. Moving the\nrule towards torch would make today's code, which raises the flag with `claim()`,\n**quietly lose parameters** — writing the divergence down is cheaper. `parity.ts` pins\nthese two by value.",
  "src": "ac94316596c7"
 },
 "nn/ParameterDict": {
  "en": "A named bundle of parameters. It exists for the same reason as `ParameterList`.",
  "src": "019f4c9ff478"
 },
 "nn/ParameterList": {
  "en": "A list of trained tensors. **Without this there is no substitute.**\n\nA parameter not attached to a layer is caught nowhere unless `ownParameters` is written\nby hand. Uncaught means the optimizer cannot see it, unseen means it is not updated,\nand yet **the loss comes down** — the remaining parameters compensate. No exception, no\nwarning.",
  "src": "4f5d0764c970"
 },
 "nn/RMSNorm": {
  "en": "**It does not subtract the mean.** That is the only difference from `LayerNorm`.",
  "src": "58239ea7029b"
 },
 "nn/RNNKind": {
  "en": "Recurrent networks — `RNN`, `LSTM` and `GRU` as one class differing only in gate count.\n\nInput is `(length, batch, features)` — torch's default, not `batch_first`.\n\n**The time axis is sequential and cannot be unrolled.** One step's output is the next step's\ninput, so there is nowhere to run in parallel, and a kernel is called per step. It holds for\nshort sequences; longer, and the per-step call cost buries the computation.",
  "src": "0b3cbce7ac1b"
 },
 "nn/Recurrent.run": {
  "en": "Produces the full output and the final state together.",
  "src": "140c0cdead68"
 },
 "nn/Sequential": {
  "en": "Layers stood in a row. The position index is the name.",
  "src": "fbdb6cd00f18"
 },
 "nn/Sequential.constructor": {
  "en": "**Layers are simply listed** — `new Sequential(a, b, c)`. That is torch's shape.\n\nTaking a single array made the very first example in `index.ts` wrong. Someone copying\ntorch code across leaves the brackets off by default, and the default should be the one\nthat is right. An array is still accepted — there is already code written that way.",
  "src": "6316184c1ced"
 },
 "nn/Sequential.namedChildren": {
  "en": "**Addressed by position index** — `0.weight`. That is torch's `Sequential`'s shape, and\nthe golden cases put weights in by that name.\n\nThe default implementation uses field names, and our layers live inside an array\n(`layers`) which that does not catch. That would leave `state_dict` entirely empty, so\nthis has to be overridden.",
  "src": "b230be562080"
 },
 "nn/Softmax2d": {
  "en": "Softmax **along the channels** of `(N, C, H, W)`. The same as `softmax(dim=1)`.",
  "src": "dee3f21a7ab9"
 },
 "nn/TripletMarginWithDistanceLoss": {
  "en": "A triplet loss taking a distance function. The default is the pairwise distance, so it gives the same answer as the one above.",
  "src": "d1f77f80dae2"
 },
 "nn/Unflatten": {
  "en": "The opposite of `Flatten`. Spreads one axis into several.",
  "src": "73ac726dbf53"
 },
 "nn/Upsample": {
  "en": "Enlargement. **The first position is `size`**, as in torch.\n\nPutting the scale factor first makes `new Upsample(2)` ambiguous between doubling and\n\"output of 2\", and the shape is plausible enough that only values catch it.",
  "src": "11d8f2f21609"
 },
 "nn/affineGrid": {
  "en": "The sampling grid `theta` draws. `(N, 2, 3)` → `(N, H, W, 2)`.\n\nThe last axis is in **`(x, y)` order** — flipped relative to the shape's `(H, W)`. On a\nsquare it gives the same answer written either way, so it only surfaces when asked with a\nrectangle.",
  "src": "4ad52eba5309"
 },
 "nn/batchNorm": {
  "en": "The function form of `BatchNormND`. **The layer calls this** — one copy of the formula.\n\n**In training it modifies the running statistics in place.** torch does, and the tensor\nyou hand in comes back updated. Returning a new one leaves the caller's buffer unmoved, so\ntraining runs and only eval-mode values are wrong.\n\n**It uses variance two ways.** Normalisation uses the biased estimate; the running-statistics\nupdate uses the unbiased one. Merge them and it diverges in eval mode only.",
  "src": "d9e508b017f8"
 },
 "nn/ctcLoss": {
  "en": "A loss that joins audio to text **without aligning positions.**\n\n`logProbs` is `(T, N, C)` — **time first.** Each sample has its own length and that is\nthe point of this loss, so it receives two sets of lengths alongside.\n\n## It adds one term whose value is zero and whose gradient is not\n\nThe gradient torch flows into `logProbs` is not the true derivative — finite differences\ngive `-γ` while torch produces `exp(logProbs) - γ` (measured: `tests/probe_ctc3.py`). At\nthe place it is used there is a `logSoftmax` in front, which makes the two the same\nanswer (it is a fixed point of that backward), but with `logProbs` as a leaf directly\nthe numbers diverge. Matched — with **why it is matched** written down.",
  "src": "2fae14f93b75"
 },
 "nn/embedding": {
  "en": "Selects rows from a table by index. Where `F.embedding(indices, table)` goes.\n\n**It is the definition itself** — the same thing `indexSelect` does, and that side already\nknows the gradient. An index appearing several times accumulates into that row several\ntimes. This is **naming something that exists**, not imitating something that does not, so\nthere is no place for the values to diverge.\n\nAnd yet this name was missing for a long time. The binding held the same three lines in\nPython and the golden cases went through that side, so it was a name absent only for those\nwriting TypeScript — the place `tests/test_binding_fills_in.py` points at.\n\n**There is no `nn.Embedding` layer yet.** None of the three has it and the golden cases do\nnot ask. It would be a place that wraps this function and holds the weights rather than a\nnew computation, so writing down that it is absent is better than putting the name there\nalone.",
  "src": "4b6c91dd9b14"
 },
 "nn/embeddingBag": {
  "en": "One row per bag. Selecting from the table **and combining** is one function.\n\nGiven `offsets` it cuts a one-dimensional index run into bags — the place where bag lengths\ndiffer. `perSampleWeights` is used by torch only when `mode='sum'`.",
  "src": "93d189f7e104"
 },
 "nn/gridSample": {
  "en": "Lifts values from where the grid points. The counterpart of `affineGrid`.\n\n**The positions are constants and the weights are tensors.** A floored integer has no\nderivative and its remainder becomes the weight, so keeping only the weights in the graph\nlets gradient flow to both the input and the grid.",
  "src": "4e8c7a1c841d"
 },
 "nn/gumbelSoftmax": {
  "en": "Picks one at random **while letting derivatives through.**\n\nDrawing one category is not differentiable, so Gumbel noise is added and `softmax` smooths\nit. The smaller `tau`, the more it concentrates on one side.\n\nWith `hard=true` the answer is 0/1 but **the gradient is the smooth one's** — the common\n`hard - soft.detach() + soft` trick, where the value is hard and the derivative is soft.\nKeeping those two apart is what this function means.",
  "src": "d6256c50133c"
 },
 "nn/multiHeadAttentionForward": {
  "en": "The function form of attention. Weights come from outside.\n\n**Input is `(L, N, E)` — length first.** torch's function of the same name is, and calling it\nwith batch first quietly mixes the wrong axes.\n\n**The mask is an added float.** A boolean table is not accepted here — `-inf` is added so\nthat softmax produces zero, rather than multiplying by zero, and multiplying after\nnormalisation leaves the remaining slots not summing back to 1. Turning booleans into floats\nhappens where torch's contract is imitated (the Python binding).",
  "src": "0c7c1ebe9a23"
 },
 "nn/scaledDotProductAttention": {
  "en": "The core of attention. **The computation `MultiheadAttention` was doing inside, given a name.**\n\nThe layer existed and this function did not. Code that writes attention by hand rather\nthan using the layer calls this name, and that is the basic form of modern transformer\ncode.\n\n**A mask is added, not multiplied.** A large negative number is added so that softmax\nproduces zero — it is not a multiplication by zero, because multiplying after softmax has\nalready normalised leaves the remaining slots not summing back to 1.\n\nWith a batch axis it runs per sample. That is because `mm` is two-dimensional; when a\nbatched matrix multiply exists, this is the place to change.",
  "src": "73a48d12feef"
 },
 "optim/": {
  "en": "Optimizers and learning-rate schedules.\n\n## Parameters are modified in place\n\nIt does not make a new tensor and swap it in. The handle the model holds and the\nhandle the optimizer sees have to be the same one; swapping splits them and produces\na state where **training runs and the parameters do not move.** In-place modification\nis blocked on a leaf with gradient enabled, so it happens inside `no_grad` — which is\nexactly what torch's optimizers do.\n\n## State is held as tensors\n\nSomething like a momentum buffer **must not hold the gradient tensor itself.** When\nthat gradient is replaced on the next step, the buffer points at the wrong thing —\na place this repository missed while only ever looking at SGD without momentum. So\nthe buffer starts as its own copy.",
  "src": "33807b83847d"
 },
 "optim/ASGD": {
  "en": "Averaged SGD. **The learning rate decays on its own each step**, and from some point on\nthe parameters are averaged.\n\n`eta` decays as `lr / (1 + lambd·lr·step)^alpha`, and `mu` is the weight of the\naverage. The default `t0` is a million, so in normal training `mu` is always 1 and\n`ax` is a copy of the parameters — the averaging branch only really runs once `t0` is\nlowered.\n\n**The decay is multiplicative.** `param *= (1 − lambd·eta)` happens first, and the\ngradient is subtracted after.",
  "src": "1b01036c3ecf"
 },
 "optim/Adadelta": {
  "en": "**The learning rate is barely used.** It builds the step size out of the history of its own updates.",
  "src": "f0c88f34532d"
 },
 "optim/Adafactor": {
  "en": "Adam, but holding the second moment **split into rows and columns.**\n\nAdam holds one variance per parameter, so its memory costs as much as the weights.\nHere only the row means and the column means are held and the outer product restores\nthem — `R + C` where `(R, C)` would have been.\n\n**One-dimensional parameters are not split** — there is only one axis to split, so the\nvariance is held directly. Ask with vectors alone and the whole point of this\noptimization never runs.",
  "src": "3ad6760a6b35"
 },
 "optim/Adafactor.extendState": {
  "en": "**The base's default cannot be used.** This optimizer's state is not parameter-shaped\nbut folded into rows and columns, and which of the two is held depends on the rank.\nThe slot count matches the parameters (the unused side is filled with `null`), so the\nindexing still works.\n\nThe constructor and `addParamGroup` both pass through this one place — written in two\nplaces, one of them eventually gets fixed alone.",
  "src": "df9775c30637"
 },
 "optim/Adagrad": {
  "en": "**Keeps adding** squared gradients — it only ever shrinks, never grows.",
  "src": "9ecfeeb22d6e"
 },
 "optim/Adamax": {
  "en": "Adam with the second moment taken as **the maximum instead of the root mean square.**",
  "src": "0f03b14af695"
 },
 "optim/ChainedScheduler": {
  "en": "Applies several **at once.** Their factors multiply.",
  "src": "fb987b33ce2a"
 },
 "optim/ConstantLR": {
  "en": "**Holds it cut until `totalIters`, then returns to the original.** The simplest form of a warm-up.",
  "src": "423f60f71850"
 },
 "optim/CosineAnnealingWarmRestarts": {
  "en": "Descends as a cosine and then **returns to the start.** Each period is `tMult` times longer.",
  "src": "f55a545212a6"
 },
 "optim/CyclicLR": {
  "en": "**With more than one group, the bounds become relative.** torch takes `base_lr` and\n`max_lr` as a list per group; here they are single numbers. Following the base's rule,\nthe value computed against the first group is multiplied by each group's baseline\nratio, so group i cycles between `baseLr·rᵢ` and `maxLr·rᵢ`. That is what someone who\nset per-layer learning rates expects, but it **is not the same as torch.** With one\ngroup the ratio is 1 and there is no difference.",
  "src": "a4ff75b453c1"
 },
 "optim/ExponentialLR": {
  "en": "**It is recursive** — it multiplies the current learning rate. It does not recount from the original.\n\nUsed alone the two produce the same sequence. What diverges is another scheduler\ntouching the same lr — overlapped through `ChainedScheduler`, the recursive form\nstacks on top of the other's result while the closed form overwrites what the other\ndid.",
  "src": "4c9d81a450db"
 },
 "optim/LRScheduler": {
  "en": "Learning-rate schedules.\n\n**It is all floating-point arithmetic, so the values have to equal torch's exactly** —\nthere is no room for an approximation. So the golden cases froze not one value but\n**the whole trajectory**, and doing that is how the core caught the difference in\n`StepLR`.\n\n**The baseline is `initialLr`, not the lr at construction time.** It is stamped onto\nthe optimizer once, and schedulers built later see the same baseline. Used alone the\ntwo are equal and nothing catches, but chained, the second one takes as its baseline\nwhat the first has already cut.",
  "src": "445ba50bd59d"
 },
 "optim/LRScheduler.current": {
  "en": "The learning rate now. Where a recursive scheduler reads the value before its own.",
  "src": "fd91500c3b37"
 },
 "optim/LRScheduler.loadStateDict": {
  "en": "The undo of `stateDict()`. It does not touch the learning rate — the optimizer holds that.",
  "src": "19fb9b9388b0"
 },
 "optim/LRScheduler.restart": {
  "en": "Makes it step from the beginning again, for when `SequentialLR` hands over.",
  "src": "98b257d9b1e0"
 },
 "optim/LRScheduler.start": {
  "en": "**Applies epoch zero.** torch does this in the constructor and here it cannot be — a\nTypeScript subclass's fields are filled in **after** `super()` returns, so calling\n`compute` inside the constructor finds things like `factor` still `undefined`.\n\nSo it is called once, right after construction. Something like `ConstantLR`, which\nchanges the value from epoch zero, loses its entire first term without it — measured\nat a maximum difference of 2.0e-01.",
  "src": "8bab3046dff5"
 },
 "optim/LRScheduler.stateDict": {
  "en": "What resuming needs — **which epoch, and the baselines.**\n\nRestoring the optimizer's `lr` alone is not enough. A scheduler is a thing that\nrecomputes its value from `epoch`, so leaving that at zero and carrying on means\n**the next `step()` puts the learning rate back to its first value** — the optimizer\nside was restored perfectly and one step erases it.\n\nThe baselines travel too. `initialLr` comes from the optimizer, but that is **the value\nthe first scheduler stamped**, and chained schedulers hold the baseline each of them\nsaw.",
  "src": "847b1aff56cf"
 },
 "optim/LinearLR": {
  "en": "Moves **in a straight line** from the starting factor to the ending factor.\n\nIt meets `ConstantLR` at the end — past `totalIters` both are the original learning\nrate. The last value alone cannot separate them, so the golden cases ask about the\nwhole trace.",
  "src": "288604a59933"
 },
 "optim/MultiStepLR": {
  "en": "Cuts only at the milestones. **It is recursive** — fixed for the same reason as `StepLR`.\n\nA milestone written twice (`[3, 3]`) multiplies twice at that point — it did under the\nclosed form too, and torch does the same.",
  "src": "855c3301e6ba"
 },
 "optim/MultiplicativeLR": {
  "en": "**Multiplies as it goes** — the baseline is the current learning rate, not the original.",
  "src": "31125a9b89e4"
 },
 "optim/NAdam": {
  "en": "Adam with Nesterov's look-ahead attached.\n\n**The momentum coefficient changes each step, and the running product of that sequence\nhas to be carried.** Held constant, the first few steps diverge quietly.",
  "src": "82c9be511db2"
 },
 "optim/OneCycleLR": {
  "en": "Up and then down. **Close to the default of a modern training recipe.**\n\nThe initial learning rate is `maxLr/divFactor` and the end is that divided again, so\n**the learning rate given to the optimizer is never used** — it is overwritten the\nmoment this is built.",
  "src": "1279b0cd931b"
 },
 "optim/Optimizer.addParamGroup": {
  "en": "Adds a group later. Where `torch.optim.Optimizer.add_param_group` goes.\n\n**It grows the state bank too.** Without that, the next step blows up with \"no state\nfor parameter N\" — the bank is indexed by the flattened parameter position.\n\nA scheduler already built does not know this group's baseline. torch asks for\n`initial_lr` in the same place — if the scheduler came first, build it again.",
  "src": "38d4e6f57a5b"
 },
 "optim/Optimizer.counters": {
  "en": "The scalars this optimizer holds. Subclasses override it.\n\n**The bank does not go in here** — the base already holds it, so `stateDict` picks it\nup on its own. What belongs here is the non-tensor things such as `stepCount`, and\nleaving one out means **bias correction restarts from the beginning** on resume. The\nvalues come out slightly different with nothing pointing at where they diverged.",
  "src": "94f2741fd1ea"
 },
 "optim/Optimizer.extendState": {
  "en": "Grows the state when a group is added.\n\nBy default it adds one **parameter-shaped** slot per registered bank. An optimizer for\nwhich that premise does not hold (`Adafactor` holds variance folded into rows and\ncolumns) overrides here.",
  "src": "ba805e06afd9"
 },
 "optim/Optimizer.grouped": {
  "en": "What this group set for itself, or the optimizer's default if it set nothing.",
  "src": "7faa3ec1a4d2"
 },
 "optim/Optimizer.loadCounters": {
  "en": "The undo of the above. Whoever overrode it takes their own back out.",
  "src": "80f88098e9c1"
 },
 "optim/Optimizer.loadStateDict": {
  "en": "Restores what `stateDict()` gave. Call it **after rebuilding the optimizer with the same arguments.**\n\nIt throws if a bank slot is missing or a size differs. Adding or removing parameters,\nor laying an old checkpoint over a model, can otherwise leave training running with\nvalues shifted by one slot, and that is far worse than an exception.",
  "src": "790407fef50f"
 },
 "optim/Optimizer.lr": {
  "en": "The learning rate of the group currently being stepped.",
  "src": "96f6022a20ad"
 },
 "optim/Optimizer.paramGroups": {
  "en": "torch's shape — this is where a scheduler edits `lr`.",
  "src": "57c320a35897"
 },
 "optim/Optimizer.params": {
  "en": "Every group's parameters, concatenated. The state bank is indexed by this position.",
  "src": "ea4b6cd178b8"
 },
 "optim/Optimizer.state": {
  "en": "One state bank. **Going through here is what makes it grow when a group is added.**\n\nIt used to be built per optimizer with `params.map(...)`, which leaves the slot for a\nlater-added parameter empty.",
  "src": "e336159ad16e"
 },
 "optim/Optimizer.stateDict": {
  "en": "Everything needed to carry on training.\n\n**Momentum alone is not enough.** The group's learning rate (a scheduler may already\nhave cut it) and the step counter have to travel with it for the step after a resume\nto give the same number as one that never stopped.\n\nTensors and numbers come back separately — the safetensors body carries tensors and\nthe header carries numbers (`serialize.ts`).",
  "src": "a4288fdb7823"
 },
 "optim/Optimizer.zeroGrad": {
  "en": "Empties the gradients. **It returns them to `null`** — filling with zeros blurs the leaf test.",
  "src": "a3dc814623fe"
 },
 "optim/ParamGroup": {
  "en": "One bundle of parameters and the hyperparameters attached to it.\n\n**There used to be no `params`.** The name is torch's `param_groups`, but it was\nalways a single `[{ lr }]` and every scheduler only ever looked at `[0]` — no\nper-layer learning rate, no excluding bias and norm from weight decay, while the name\nsaid otherwise. torch's shape with nothing inside is worse than nothing: a user\ncalling `paramGroups.push(...)` was quietly ignored.",
  "src": "6f11ab8ebed0"
 },
 "optim/ParamGroup.initialLr": {
  "en": "The value a scheduler takes as its baseline. The first scheduler stamps it once.",
  "src": "dbd8ecdcdfe5"
 },
 "optim/ParamGroup.params": {
  "en": "The parameters this group steps.",
  "src": "a795576b1ff3"
 },
 "optim/ParamGroup.weightDecay": {
  "en": "For when this group alone uses a different value. Absent, it is the value given when the optimizer was built.",
  "src": "ee3085f9bc09"
 },
 "optim/ParamGroupInit": {
  "en": "What goes in when a group is made. Leave `lr` out to use the optimizer's default.",
  "src": "32025d3a5b74"
 },
 "optim/ParamsArg": {
  "en": "What an optimizer's constructor accepts — a list of tensors or a list of groups.",
  "src": "0dec45569a90"
 },
 "optim/PolynomialLR": {
  "en": "Descends as `(1 − t/T)^power`. At `power=1` it is a straight line.",
  "src": "9bff371d1930"
 },
 "optim/RAdam": {
  "en": "Adam, but **without the adaptive step size early on.**\n\nAdam is known to be jumpy at the start because the second moment has few samples and\nits variance is large; this one passes through that stretch like SGD. Drop the\nthreshold (`rho > 5`) and the values become Adam's.",
  "src": "4df2b8d19927"
 },
 "optim/ReduceLROnPlateau": {
  "en": "Cuts the learning rate when the value stops improving.\n\nUnlike other schedules it **has to be given a value** to move — hence `step(metric)`.",
  "src": "e41a10154997"
 },
 "optim/ReduceLROnPlateau.stateDict": {
  "en": "The two things a resume needs. **`best` starts at infinity** — which is why putting\nthese numbers in the header needed somewhere to write infinity other than\n`JSON.stringify` (`numbersToMeta` in `serialize.ts`).\n\nWithout restoring them, whatever value arrives right after a resume becomes \"the first,\ntherefore the best\", and the patience counter resets.",
  "src": "1c7638b7dc0b"
 },
 "optim/Rprop": {
  "en": "Looks at **the sign of the gradient only.** The magnitude is unused, and the step width\ngrows and shrinks per slot.\n\nIf the sign holds, the width is multiplied by `etaPlus`; if it flips, by `etaMinus`.\n**A flipped slot does not take that step at all** — its gradient is set to zero, which\nalso makes the next step's \"previous gradient\" zero. Without those two, inputs whose\nsign never flips produce a difference that is never caught.",
  "src": "3d39922a7e73"
 },
 "optim/SequentialLR": {
  "en": "**Chains schedulers end to end.** On reaching a milestone it hands over to the next.",
  "src": "d7ed682bb1f2"
 },
 "optim/StepLR": {
  "en": "Multiplies by `gamma` every `stepSize` epochs.\n\n**It is recursive** — the same reason `ExponentialLR` below writes down. This used to\nbe the closed form `base * gamma ** floor(epoch / stepSize)`.\n\nStepped alone from the beginning, the two produce **the same sequence.** That is why\nthe golden cases, which freeze the whole trace, were green for a long time. What\ndiverges is building a new scheduler on an optimizer whose lr has already moved —\n**resuming training.** At that moment the closed form puts the learning rate back to\nits first value (0.05 to 0.2). No error appears; the loss just jumps once.",
  "src": "87d3603ddc52"
 },
 "random/gauss": {
  "en": "One sample from the standard normal. Box–Muller.\n\n**Of the two values it produces, one is used and one is thrown away.** Caching the\npair would mean the cache has to be cleared whenever a seed is planted, and if it\nis not, a value from just before the planting appears just after it — a state that\nlooks rewound but whose first number is wrong, which is the hardest kind to find.",
  "src": "401a3fafe543"
 },
 "random/manualSeed": {
  "en": "Plants the seed and rewinds everything registered with it.\n\n**It turns 0 into 1.** xorshift emits zero forever once its state is zero — nobody\nexpects `manualSeed(0)` to be the call that kills randomness.",
  "src": "dfda1d1dc2bd"
 },
 "random/onSeed": {
  "en": "Registers something to be rewound when a seed is planted. **It receives the seed value.**\n\nIt was first called with no argument, and the dropout counter always rewound to 1.\nThe promise that the same seed gives the same result still held, but **different\nseeds stopped giving different results** — someone running five seeds to measure\nvariance would not know the dropout masks were identical all five times, and would\nread a number shaken only by weight initialisation as experimental variance.",
  "src": "c65d3d275e2e"
 },
 "random/uniform": {
  "en": "One sample from the uniform distribution on `[0, 1)`.",
  "src": "20a317abaf96"
 },
 "random/uniformArray": {
  "en": "An array filled from the uniform distribution on `[-bound, bound]`. Layer initialisation uses it.",
  "src": "6a56acf1da06"
 },
 "rnn/": {
  "en": "The eight top-level recurrences — `torch.lstm`, `torch.gru`, `torch.rnn_tanh`,\n`torch.rnn_relu`, and their `_cell` counterparts.\n\n**The only difference from the layer (`nn.Recurrent`) is where the weights come\nfrom.** The layer holds its own; this side **receives them as a list.** torch offers\nboth, and what the layer calls internally is this side.\n\nSo the step equations live here in one copy. Two copies, here and in the layer,\nmeans a day when **the gate order diverges**, and then the shape is right and only\nthe values are wrong — this repository already shares the equations between cell\nand layer for that same reason.\n\n## Gate order\n\nThe **row order** of `weight_ih` is the convention (measured):\n\n    LSTM  i, f, g, o          GRU  r, z, n          RNN  (just one)\n\nChange it and only the values diverge; the shape stays.",
  "src": "08cd868490b0"
 },
 "rnn/lstm": {
  "en": "`torch.lstm` — **it spreads three** (`output, h_n, c_n`), rather than bundling them the way the layer does.",
  "src": "6dde300291f6"
 },
 "rnn/rnnApply": {
  "en": "Several steps. `hx` is `(layer, batch, H)`.\n\n**Bidirectional and inter-layer dropout are refused.** Half-imitating something that\nis not there catches loudly for the first — the shape comes out halved — but\n**dropout diverges with plausible values** (training with no regularisation applied).\nBoth stop here.",
  "src": "c451e73596f5"
 },
 "rnn/rnnStep": {
  "en": "One step. **All four kinds pass through here.**",
  "src": "12cb1032d090"
 },
 "serialize/": {
  "en": "Checkpoint to bytes, bytes to checkpoint — the **safetensors** format.\n\n## Why this is needed\n\n`stateDict()` existed, but **there was no way to save it.** Refreshing the tab lost\nthe training. A browser is a place where the process dies far more often than on a\ndesktop, so this gap was felt more sharply here than it is in torch.\n\n## Why no format of our own\n\ntorch's `save`/`load` is pickle. It is a format that unpacks by executing Python\nobjects, which cannot be carried into a browser and should not be.\n\nsafetensors is a JSON header and a body of contiguous bytes. **The codec is this\none file, and for that price Python `borch`, numpy and the HF tools read the same\nfile.** A private format loses that — training in the browser and taking the result\nback to your own machine is half of this project's story, and a format only we can\nread throws that half away.\n\n```\n[8 bytes LE u64: header length N][N bytes of JSON header][body: tensor bytes, concatenated]\n```\n\n## Writing the dtype honestly\n\nborch's `int64` and `bool` are **labels; the values live in a float32 buffer**\n(`dtype.ts`). Writing `I64` in the header contradicts a four-byte body and\n**breaks other people's readers.**\n\nSo `dtype` is always written as `F32`, and borch's label rides separately in\n`__metadata__`. Someone else reads a float32 array (which is true); we read it and\nthe label comes back too.",
  "src": "d33ef8085fe1"
 },
 "serialize/load": {
  "en": "Bytes, as a checkpoint. **Synchronous** — uploading is one queued write.\n\nIt will not quietly manufacture odd tensors out of a damaged file. Lengths, offsets\nand types are all checked, and it throws when they do not agree — a checkpoint that\nis quietly wrong robs every training run after it of meaning.",
  "src": "961c2e21db0f"
 },
 "serialize/metaToNumbers": {
  "en": "The undo of the above. Only entries whose prefix matches are returned.",
  "src": "b112ca6aad92"
 },
 "serialize/numbersToMeta": {
  "en": "Numbers as strings, so they can ride in the header. safetensors' `__metadata__` takes strings only.\n\n**It writes with `JSON.stringify`.** `String(0.1)` is `\"0.1\"` and reading that back\ngives the same double, but `Infinity` becomes `\"Infinity\"`, which `JSON.parse`\nrefuses — and `ReduceLROnPlateau`'s `best` starts at exactly infinity.",
  "src": "e0dec75d554d"
 },
 "serialize/prefixed": {
  "en": "Puts a tag in front of each name. Used to hold a model and an optimizer **in one file.**\n\n```ts\nconst bytes = await save({\n  ...prefixed(\"model\", model.stateDict()),\n  ...prefixed(\"opt\", opt.stateDict().tensors),\n}, { ...numbersToMeta(\"opt\", opt.stateDict().numbers) });\n```",
  "src": "d7dd3806bb81"
 },
 "serialize/save": {
  "en": "A checkpoint, as bytes.\n\n**It is async** — the values are on the GPU, so there is a round trip to fetch them\nback. Hand it tensors already brought down to the host (`await t.cpu()`) and that\nround trip is gone.",
  "src": "e92c8000b70a"
 },
 "serialize/unprefixed": {
  "en": "Takes the tag off. Anything with a different prefix is not returned.",
  "src": "1eeedec1c73a"
 },
 "special/": {
  "en": "Special functions — the incomplete gamma and the polygammas.\n\n**The reason these kernels live apart** is that `n` is a shader constant.\n`polygamma(n, x)` bakes a different shader per `n`, and the unary table (`UNARY` in\n`kernels.ts`) hangs one shader off one name, so it does not fit there.\n\nThe formulas are carried over from the core (numpy) unchanged — two copies means a\nday when only one gets fixed. The core side was confirmed against torch for both\nvalues and gradients first; this is that carried across.",
  "src": "02d37639f7a1"
 },
 "special/igammac": {
  "en": "The upper branch, `Q = 1 − P`. **The two sum to exactly 1** (measured).",
  "src": "3cd286c74fd5"
 },
 "tensor/": {
  "en": "`Tensor` — one GPU buffer, a shape, and one node on the tape.\n\nThe layout is **NCHW** and the dtype is **float32, only.** The sister project\n(`borch_webgpu`) holding NHWC and packing int64 into float32 was a detour around\nTF.js's constraints, and our kernels do not have those constraints. Imitating it\ninherits someone else's detour for no reason.",
  "src": "ba0a08e93aa7"
 },
 "tensor/AtIndex": {
  "en": "What can go into one axis of `at()`. The syntax is written down in `indexing.ts`.",
  "src": "b93ca59e9575"
 },
 "tensor/PadMode": {
  "en": "The four things `F.pad` accepts.",
  "src": "6fb8513262cf"
 },
 "tensor/Reduction": {
  "en": "How a loss folds. **Only `KLDivLoss` takes a fourth (`batchmean`).**",
  "src": "174b105e7b96"
 },
 "tensor/Scope": {
  "en": "One open scope. `using` closes it at the end of the block.",
  "src": "533f927e666a"
 },
 "tensor/Scope.keep": {
  "en": "Keeps something alive past this scope's close. The same work as the callback form's second argument.\n\nWhat differs from `keepAlive(t)` is **where it is tied.** That one keeps it alive forever; this hands\nit to the enclosing scope — closing that releases it too.",
  "src": "32b95f698d80"
 },
 "tensor/Tensor": {
  "en": "The types of the methods hung from the table above. Paired with the loop above; fix one\nalone and they fall out of step.",
  "src": "7f71dad4a010"
 },
 "tensor/Tensor.adaptiveAvgPool": {
  "en": "The old name. The same as `adaptivePool(\"avg\", …)` — kept because there are callers.",
  "src": "98a0eaa3ed84"
 },
 "tensor/Tensor.adaptivePool": {
  "en": "Folds to a fixed output size. The window size comes out of the input.\n\n**It does not have to divide evenly.** It used to refuse, and torch takes a different window per\nposition — the refusal was not a form of imitation but a different rule. The start floors and the\nend ceils, so reducing 8 to 3 gives windows of 3·3·2.\n\nIt folds one axis at a time. The window is rectangular, so doing it per axis gives the same value —\neach row has the same length, which makes the mean of means the overall mean, and the maximum is\nthat way anyway.\n\nNo dedicated kernel is used. With a window that differs per position there is nothing to bake into\nthe shader as a constant, and adaptive pooling is usually a single final call rather than something\nrunning per step.",
  "src": "d8caa56c95ee"
 },
 "tensor/Tensor.add": {
  "en": "An argument torch offers that was missing here. Without it the caller writes it out as\n`x.add(y.mul(a))` for the same answer, while **the in-place form (`add_`) already had it** —\nso two names were taking different things.",
  "src": "3ce8b9eb5a8b"
 },
 "tensor/Tensor.addbmm": {
  "en": "**It merges the batch** — multiplies, then sums the batch axis to give two dimensions.\n\nOne character from `baddbmm` and a different result rank. With a batch of 1 the two look the\nsame, so the cases keep the batch above one.",
  "src": "e435d0724db6"
 },
 "tensor/Tensor.addcdiv": {
  "en": "`this + value·(t1 / t2)`. The form optimizers use to write an update.",
  "src": "a0e1366a879e"
 },
 "tensor/Tensor.addcmul": {
  "en": "`this + value·(t1 · t2)`. **There is no `beta`** — `this`'s coefficient is always 1.",
  "src": "4ed89ff60095"
 },
 "tensor/Tensor.addmm": {
  "en": "`β·this + α·(mat1 @ mat2)`. `this` spreads to the result's shape.",
  "src": "33308331a40b"
 },
 "tensor/Tensor.addmv": {
  "en": "`β·this + α·(mat @ vec)`. The result is one-dimensional.",
  "src": "3d0c007994ac"
 },
 "tensor/Tensor.addr": {
  "en": "`β·this + α·(vec1 ⊗ vec2)`. An outer product, so the result is two-dimensional.",
  "src": "9db7b194002b"
 },
 "tensor/Tensor.adjoint": {
  "en": "The conjugate transpose of the last two axes. On reals it is the transpose.",
  "src": "f9cf68ae5432"
 },
 "tensor/Tensor.all": {
  "en": "Whether all are true, or whether any is. It answers 0/1.",
  "src": "8f94a82e0e07"
 },
 "tensor/Tensor.allclose": {
  "en": "The golden harness does **not** turn this on — turned on, NaN passes where NaN must not. Having the\nargument and turning it on are different places: whether to is the caller's decision.",
  "src": "9905379b8918"
 },
 "tensor/Tensor.alphaDropout": {
  "en": "The dropout used with SELU. **It does not put zeros where it drops.**\n\nIt puts a negative constant and applies an affine transform over the whole so that mean and\nvariance are preserved — zeros break SELU's self-normalisation, and the values are plausible\nenough that nothing shows while training runs.",
  "src": "c60d4926c274"
 },
 "tensor/Tensor.aminmax": {
  "en": "The minimum and maximum **together.** Asking for one alone would not catch the other being wrong —\nthat is why torch gives this name separately.",
  "src": "6500b4a7048a"
 },
 "tensor/Tensor.angle": {
  "en": "The argument. For a complex, `atan2(im, re)`; for a real, π on negatives (the type is always real).",
  "src": "a5ee4bb91e8c"
 },
 "tensor/Tensor.arange": {
  "en": "`[start, end)` in steps of `step`. **The end is excluded.**\n\nGiven one argument it is `[0, n)`, as in torch, and for a long time that was the only form.\nWhile start and step could not be given, callers imitated it with\n`arange(n).mul(...).add(...)`, which is a different computation whose rounding accumulates\ndifferently.",
  "src": "2c4278aeedc5"
 },
 "tensor/Tensor.argmax": {
  "en": "**Where** the maximum or minimum is. On a tie, the first position.",
  "src": "017514c35800"
 },
 "tensor/Tensor.argsort": {
  "en": "The sorted positions only. Used when the values are not needed.",
  "src": "0842969e829d"
 },
 "tensor/Tensor.argwhere": {
  "en": "The same as `nonzero` — torch carries both names.",
  "src": "31fba9343b1a"
 },
 "tensor/Tensor.asStrided": {
  "en": "Reads flat storage **with different strides.** torch gives a view; here it is a copy.",
  "src": "d96678235215"
 },
 "tensor/Tensor.at": {
  "en": "Where the brackets go. One door for carrying `x[...]` across — the syntax and the reasoning are in `indexing.ts`.\n\n```ts\nx.at(0)                     // x[0]           the axis disappears\nx.at([null, 1])             // x[:, 1]        null is Python's `:`\nx.at(slice(1, 3))           // x[1:3]         the axis remains\nx.at([0, slice(1, 3)])      // x[0, 1:3]\nx.at(slice(null, null, 2))  // x[::2]\nx.at([[0, 2]])              // x[[0, 2]]      two brackets — numpy's shape\nx.at(idx)                   // x[idx]         an int64 tensor\n```\n\n**The outermost array is always the list of axes.** Give fewer and the remaining axes come\nwhole.\n\n## No values are made here\n\nEverything is handed to existing methods — an integer to `select`, a contiguous span to\n`narrow`, a strided one or an index list to `indexSelect`. There is no new kernel, so **the\ngolden cases already protect those values.** What this method is responsible for is not the\nvalue but **which door it is sent through.**\n\n## Boolean masks are not accepted\n\n`x[mask]` remains `await x.maskedSelect(mask)`. The length of the result **depends on the\nvalues**, so it takes one read off the GPU to know, and making `at()` asynchronous for that\nalone would put an `await` on every other use for no reason. It is the same reason `unique`,\n`nonzero` and `bincount` are asynchronous, and keeping it in the same place is better.",
  "src": "13c4e79a3433"
 },
 "tensor/Tensor.atleast2d": {
  "en": "At least two-dimensional. Already two or more, and it is unchanged.",
  "src": "4c5ed803bbbb"
 },
 "tensor/Tensor.backward": {
  "en": "Flows gradient.\n\n**The argument order matches torch** — `backward(gradient, retainGraph)`. It used to have\n`retainGraph` first, which made `y.backward(onesLike(y))` impossible to carry across. It is a common\nline for a Jacobian-vector product, and **the core (numpy) had taken it from the start** — this was\nthe only one of the three that could not.\n\nChanging the first position broke no call site. Nowhere in the repository passed an argument to\n`backward` at all, because the only thing that could be passed was `retainGraph`, and nobody used\nit.",
  "src": "1daa40b46f4a"
 },
 "tensor/Tensor.baddbmm": {
  "en": "**It keeps the batch.** That is where it parts from `addbmm`.",
  "src": "84709105c174"
 },
 "tensor/Tensor.bartlettWindow": {
  "en": "The triangular window. 1 in the middle, 0 at both ends.",
  "src": "853c5e6b32c0"
 },
 "tensor/Tensor.batchNorm": {
  "en": "Normalises along the batch axis. Training mode — it does not use running statistics and counts\nfrom this batch.\n\nIt differs from `layer_norm` only in which axis is folded. A different axis means changing the\nfolded axis, not standing up a separate function.",
  "src": "d0d34c30c7de"
 },
 "tensor/Tensor.batchNormFused": {
  "en": "Fused batch normalisation. Statistics, normalisation, scale and shift in three kernels.\n\nThe assembled version cost over twenty dispatches for one layer, and most of a ResNet step's 1,636\ncame from there (measured). Here it is two forward and two backward.",
  "src": "02e1c78cc9db"
 },
 "tensor/Tensor.bceWithLogits": {
  "en": "Binary cross-entropy taking logits directly.\n\n**It does not compute `sigmoid` first and take the log.** That gives `log(0)` where the model\nis confident and the loss becomes infinite. `max(x,0) − x·y + log(1+exp(−|x|))` gives the same\nvalue without overflow — that is why this function exists separately.",
  "src": "7a81969038c9"
 },
 "tensor/Tensor.bernoulli": {
  "en": "Each slot **reads its own value as a probability** and lands on 0 or 1. At `p=0` all zeros,\nat `p=1` all ones.\n\n**The comparison finishes on the device** — once the drawn randomness is uploaded, no value\ncomes back down. The binding (`borch_webgpu`) builds the same name out of numpy, but that is\nbecause `get_rng_state` over there has to serialise one stream, and there is no reason for\nthis side to do the same.",
  "src": "c4beeba4ea0c"
 },
 "tensor/Tensor.bilinear": {
  "en": "`y[o] = x₁ᵀ·W[o]·x₂ + b[o]`. The weight has **three axes.**\n\nIt does not call `einsum` — that side imports this file, so they would bite each other.\nSplit into two steps it writes down with what already exists: first ride `x₂` through `W`\nto make `(B, O, I)`, then fold with `x₁` along the last axis.",
  "src": "2d53ceadd84f"
 },
 "tensor/Tensor.bincount": {
  "en": "How many times each bucket appears.\n\n**The type diverges** (measured): without weights it is `int64`; with weights it is the weights'\ntype — counting and summing are different jobs.",
  "src": "a0bc2c097543"
 },
 "tensor/Tensor.binomial": {
  "en": "The number of successes in `this` trials. **At probability 0 it is 0; at 1 it is the trial count itself.**\n\nThe trial count is a value, so the iteration count is a value too — read for the same reason\nas `poisson`.",
  "src": "de6b3fdf681e"
 },
 "tensor/Tensor.blockDiag": {
  "en": "Blocks laid along the diagonal, zero elsewhere.",
  "src": "9b428255f213"
 },
 "tensor/Tensor.bmm": {
  "en": "Batched matrix multiply. `(B, N, K) × (B, K, M)`.\n\nIt unpacks the batch, repeats `mm`, and stacks — a separate batched kernel would mean fixing two\ncopies alongside `mm`. When batches grow large, that is the place to stand a kernel up.",
  "src": "f6adb81ee892"
 },
 "tensor/Tensor.broadcastTensors": {
  "en": "Stretches everything to a common shape. **Copies, not views.**",
  "src": "af3f437cdd75"
 },
 "tensor/Tensor.bucketize": {
  "en": "`searchSorted` **with the receiver flipped.** That is the whole difference between the two names.\n\ntorch has `bucketize(input, boundaries)` taking the values first, so the values are the\nreceiver here too. The computation exists only on the other side — two copies and one of them\ngets fixed alone.",
  "src": "6bb97efa7bcf"
 },
 "tensor/Tensor.buffer": {
  "en": "The GPU buffer holding the values. **The only door through which an operation touches them.**\n\nThis getter is the device check. There are 176 operation entry points and hanging a guard on\neach is not a thing that gets done, but all of them pass through here in the end — 75 places\ninside the repository, 6 in the optimizers, 1 in `nn`. So feeding a host-side tensor into any\noperation stops here, with torch's own wording.\n\n**Which operation it was is not recorded.** Recording that would mean editing 176 places, and\nthe stack trace already points at it.",
  "src": "0153246618c4"
 },
 "tensor/Tensor.cartesianProd": {
  "en": "Every pair. **Given only one it is just that** (measured) — it stays one-dimensional.\n\nIt does not read the values. Which slot goes where is decided **by the counts alone**, so it\ngathers by index — reading would have made this asynchronous too.",
  "src": "31d7df06c80e"
 },
 "tensor/Tensor.cat": {
  "en": "Joins along one axis.\n\n**There is no new kernel.** Padding each by the other's size and adding does it — the padded\nregion is zero and they do not overlap, so the sum is the concatenation. The backward is\n`pad`'s, used as-is. It costs twice the memory, and not making one more hand-written backward\nis worth more.",
  "src": "ebc3a934566e"
 },
 "tensor/Tensor.cauchy_": {
  "en": "The Cauchy distribution. **It has no mean** — the tails are heavy enough that the sample mean does not converge.",
  "src": "91b6f86c5998"
 },
 "tensor/Tensor.cdist": {
  "en": "The distance between every pair. **It falls out of broadcasting alone** — no kernel is added.\n\n`p === 2` is kept separate not because it is faster but because **the square root has no\nderivative at zero.** Down the general path (`|d|^p` then `^(1/p)`) the gradient becomes NaN at\nthat same place.",
  "src": "7ca23029fded"
 },
 "tensor/Tensor.celu": {
  "en": "Unlike ELU it **divides** the negative side by α before taking the exponential.\n\nAt α=1 the value equals ELU's, so measuring without giving α cannot tell them apart.",
  "src": "9bba86843aaf"
 },
 "tensor/Tensor.cfloat": {
  "en": "Real to complex. **`to(\"complex64\")` will not do it** — a complex is two f32 per slot, so relabelling\nis blocked, and that block is right. It is built by attaching a zero imaginary part.",
  "src": "fe94f9b3a4ba"
 },
 "tensor/Tensor.chainMatmul": {
  "en": "Multiplies several matrices in sequence. `multiDot` takes the same thing as a list.",
  "src": "b23662be6ab4"
 },
 "tensor/Tensor.channelShuffle": {
  "en": "Splits channels into groups and **interleaves them back.**\n\nShuffling `[0,1,2,3]` in two groups gives `[0,2,1,3]` — it is the place that releases\ninformation trapped inside groups after a grouped convolution, so the direction of the\ninterleave is the whole of the value.",
  "src": "d1f6b573c769"
 },
 "tensor/Tensor.cholesky": {
  "en": "**Written with a flip.** `Lᵀ` is `L` transposed and transposition is already differentiable, so\nthe factorisation is not written twice — the same formula in two copies eventually gets fixed in\none of them.",
  "src": "2cc97de91f8b"
 },
 "tensor/Tensor.choleskyEx": {
  "en": "The non-throwing side of `cholesky`.",
  "src": "1180eac344bd"
 },
 "tensor/Tensor.choleskyInverse": {
  "en": "From a Cholesky factor it produces **the original matrix's inverse**, not the factor's.",
  "src": "b3ca63094d7b"
 },
 "tensor/Tensor.choleskySolve": {
  "en": "Solves `A x = b` **from the Cholesky factor.** `A = L Lᵀ` (or `Uᵀ U`).\n\nIt rebuilds `A` and sends it to `solve`. Two triangular substitutions would be cheaper, but written\nthat way the backward has to be hand-written and **the gradient towards the factor goes missing\nquietly.**",
  "src": "df09e7ab8990"
 },
 "tensor/Tensor.clamp": {
  "en": "Cuts above and below.\n\n**It must not be laid on top of `maximum` and `minimum`.** Those two split the gradient in\nhalf at a tie (torch does), while `clamp` passes it whole at the boundary. Laid on top, the\ngradient halved exactly where `x` sat on the boundary.",
  "src": "298fab03aa9a"
 },
 "tensor/Tensor.clampMax": {
  "en": "Cuts from above only. torch's `clamp(max=…)`.",
  "src": "285e7fe4ba3d"
 },
 "tensor/Tensor.clampMin": {
  "en": "Cuts from below only. torch's `clamp(min=…)`.",
  "src": "e2f11ee164a1"
 },
 "tensor/Tensor.clip_": {
  "en": "The same as `clamp_` — torch carries both names.",
  "src": "b0fcf829323f"
 },
 "tensor/Tensor.clone": {
  "en": "A new tensor with the same values. The graph stays connected — that is what differs from `detach`.",
  "src": "f1c63ca2a420"
 },
 "tensor/Tensor.columnStack": {
  "en": "Stands a one-dimensional input up **as a single column** and joins. That is where it parts from `hstack`.",
  "src": "7eaa8b9e6f4a"
 },
 "tensor/Tensor.combinations": {
  "en": "Combinations of `r` at a time. **Order does not matter**, and with-replacement is separate.",
  "src": "23218094e983"
 },
 "tensor/Tensor.complex": {
  "en": "Weaves a real and an imaginary part together. Where `torch.complex` goes.\n\nThe backward **splits the incoming complex gradient as it is** — the convention *is*\n`(∂L/∂re, ∂L/∂im)`, so there is nothing to do but take it apart. This place is the definition of\nthe convention.",
  "src": "2ce1c11ee683"
 },
 "tensor/Tensor.complexAbs": {
  "en": "The magnitude of a complex. **The result is real and the gradient is `z/|z|`** — with **no**\nconjugate attached.\n\nThe real `abs` is in the unary table and does not come here. `abs()` looks at the type and\ndispatches — **that is why this is public.** The dispatch happens outside the class (after the unary\ntable is laid on), so a private one could not be called.",
  "src": "5b03ae0ad4a6"
 },
 "tensor/Tensor.cond": {
  "en": "The condition number. By default the ratio of singular values.",
  "src": "faab128fcea2"
 },
 "tensor/Tensor.conjPhysical": {
  "en": "The conjugate. **Not holomorphic, so the backward is `conj(g)`** — not of the form `conj(f')·g`.\n\n**A place it parts from torch.** torch's `conj` is lazy and raises a conjugate bit without flipping\nthe values (which is why `is_conj` is true and `view_as_real` refuses). Ours flips immediately, so\nthat state does not exist at all — the values are the same.",
  "src": "cd7a989f7a73"
 },
 "tensor/Tensor.constantPadNd": {
  "en": "The raw name for `padND(mode=\"constant\")`. **From the last axis, in (before, after) order.**\n\nIt is a place where one computation has two names, so it **does not compute here** and hands\nover — only one of the two being right has happened three times in this repository.",
  "src": "379b390cdb3d"
 },
 "tensor/Tensor.conv1d": {
  "en": "One-dimensional convolution. `(N, C, L)`.",
  "src": "b0e6507dc2ff"
 },
 "tensor/Tensor.conv2d": {
  "en": "Two-dimensional convolution. `this` is `(N, C, H, W)` and the kernel `(O, C, KH, KW)` — **NCHW.**\n\nThe sister project carried NHWC around, and that was because TF.js's conv was fast only in that\nlayout. Here we write the kernels ourselves, so torch's layout is used as-is.",
  "src": "92aed6e48952"
 },
 "tensor/Tensor.conv3d": {
  "en": "Three-dimensional convolution. `(N, C, D, H, W)`.",
  "src": "b1d2c40d461c"
 },
 "tensor/Tensor.convND": {
  "en": "Convolution independent of dimensionality. `conv1d`, `conv2d` and `conv3d` all come here.\n\nOne spatial axis is 1-D and two is 2-D — a separate function per dimensionality makes three\ncopies, and a day comes when only one of them is fixed. The sister project was in exactly that\nstate.",
  "src": "710361d3d39b"
 },
 "tensor/Tensor.convTransposeND": {
  "en": "Transposed convolution independent of dimensionality.\n\n**There is no new kernel.** A transposed convolution's forward is the same computation as an\nordinary convolution's flow towards its input, so `convNDGradInputTiled` is used as-is. The\nbackward flips likewise — the input-side gradient is an ordinary convolution's forward, and the\nweight side is the same kernel with two arguments exchanged. The same computation in two copies\nmeans a day when one of them is fixed.\n\n**The weight axes are flipped relative to `convND`** — `(in, out, …)`. With a square kernel the\nshape still matches when flipped, so only the values diverge.",
  "src": "b0225cd75c79"
 },
 "tensor/Tensor.copyFrom": {
  "en": "Moves another tensor's values into this buffer. **It does not swap the tensor.**\n\nThe optimizer holds the parameter's handle, so making a new tensor and swapping it in when putting\nweights in leaves the optimizer looking at something else — a state where training runs and the\nparameters do not move. So the slot stays and only the values move.",
  "src": "9275ff830176"
 },
 "tensor/Tensor.corrcoef": {
  "en": "The covariance divided by the standard deviations. **The diagonal comes out 1** — that is the check.",
  "src": "8a395bfc029b"
 },
 "tensor/Tensor.cosineEmbeddingLoss": {
  "en": "`1 − cos` at `y=1`, and `max(0, cos − margin)` at `y=−1`.",
  "src": "e93facd97e68"
 },
 "tensor/Tensor.cosineSimilarity": {
  "en": "How closely two bundles point the same way.",
  "src": "1e7eef4acb4b"
 },
 "tensor/Tensor.countNonzero": {
  "en": "The count of non-zeros. **It takes an axis** — this used to be a full reduction only.\n\nWithout the axis, `x.countNonzero(1)` quietly discards the argument and produces a scalar, and\nthat scalar broadcasts against anything.",
  "src": "0974e8062081"
 },
 "tensor/Tensor.cov": {
  "en": "Covariance. **Rows are variables and columns are observations** — the axes are the opposite of\nnumpy's, which makes it a confusing place.",
  "src": "1a00de7ed518"
 },
 "tensor/Tensor.cpu": {
  "en": "Brings the values down to the host. Where torch's `t.cpu()` goes, and **asynchronous** — a round trip\nto fetch GPU memory back, with no way around it.\n\n**It cuts the graph.** torch's `.cpu()` is differentiable and here it cannot be — there are no host\nkernels, so there is no path for a backward to take. Better cut than left attached and quietly\nflowing zeros.",
  "src": "151db9617b31"
 },
 "tensor/Tensor.cross": {
  "en": "The cross product of three-dimensional vectors. It splits into three axes and recombines.",
  "src": "a1797bfff55d"
 },
 "tensor/Tensor.crossEntropy": {
  "en": "Straight from logits. `log_softmax` and `nll_loss` joined.",
  "src": "75424beefe92"
 },
 "tensor/Tensor.ctcOne": {
  "en": "One sample's `-log P(target | audio)`.",
  "src": "e79dc6c5d3ba"
 },
 "tensor/Tensor.cummax": {
  "en": "The cumulative maximum and minimum. **On a tie it gives the later position** — as in torch.",
  "src": "9b8e716a03db"
 },
 "tensor/Tensor.cumsum": {
  "en": "Accumulates along one axis.",
  "src": "90e66fe21a4b"
 },
 "tensor/Tensor.cumulativeTrapezoid": {
  "en": "The cumulative version. **The last value must equal `trapezoid`** — that is the check.",
  "src": "29a6f42c9059"
 },
 "tensor/Tensor.dequantize": {
  "en": "A quantised tensor back to float. **For us it is always the identity.**\n\nThe reason this differs from \"an identity that happens to pass today\" is that having no\nquantised dtype is **already settled as permanent** — the only input it can take is float, and\non floats torch is the identity too (measured). **It is not differentiable** — torch stops at\n`backward`.",
  "src": "9af05076f825"
 },
 "tensor/Tensor.det": {
  "en": "The determinant. The backward is `det·A⁻ᵀ`.",
  "src": "c1c0edce3f8f"
 },
 "tensor/Tensor.detach": {
  "en": "A copy cut from the graph. The buffer is **shared** — for places that will only read the value.\n\n`logsumexp` uses it to detach the maximum. Undetached, `m` carries its own gradient, and\nalthough that share cancels exactly on paper, in floating point it becomes a subtraction of\nlarge numbers.",
  "src": "90a67e8d1a0a"
 },
 "tensor/Tensor.detach_": {
  "en": "Cuts the graph at **the same tensor.** torch's `detach_`.\n\n`detach()` produces a new one, so the original is still attached upstream — treating the two\nas the same means backpropagation keeps flowing through `y` even after `y.detach_()`.",
  "src": "fb6e64267d4a"
 },
 "tensor/Tensor.device": {
  "en": "Where the values are. Where torch's `t.device` goes.\n\n**`'cpu'` is a place values are held, not a device that computes.** borch has no CPU kernels\n— a tensor brought down with `cpu()` can only be read (`toArray`, `item`, `repr`) and put\nback up, and feeding it to an operation stops at the getter above with torch's own wording.\nIt is a partial implementation, but the failure is not quiet.",
  "src": "07d95a034f63"
 },
 "tensor/Tensor.diag": {
  "en": "Two-dimensional, it extracts the diagonal; one-dimensional, it lays one out — torch's `diag`.",
  "src": "9f2110267c0f"
 },
 "tensor/Tensor.diagEmbed": {
  "en": "**Spreads the last axis onto a diagonal**, adding an axis. The opposite of `diagonal`.",
  "src": "57b50060aec5"
 },
 "tensor/Tensor.diagflat": {
  "en": "A square matrix with the vector laid on its diagonal.",
  "src": "34f403cb7e84"
 },
 "tensor/Tensor.diagonal": {
  "en": "The diagonal. A positive `offset` takes the one above, a negative one the diagonal below.\n\n**Which two axes are read diverges.** `torch.diagonal` takes the first two (`0, 1`) and\n`torch.linalg.diagonal` the last two (`-2, -1`) — given three dimensions, `(2,3,4)` becomes\n`(4,2)` and `(2,3)` respectively. The names are close enough to read as the same thing while\neven the shape differs. So the axes are taken as arguments.\n\n**The extracted axis goes to the back.** The remaining axes come first and the diagonal last —\nas in torch.",
  "src": "235df8306410"
 },
 "tensor/Tensor.diagonalScatter": {
  "en": "A **copy** with the diagonal swapped out.",
  "src": "202bb2e7f947"
 },
 "tensor/Tensor.diff": {
  "en": "The difference between neighbours. Repeated `n` times it shortens by that much.\n\nPrepending one makes the result **the same length** as the input — the place used to avoid\nlosing the first slot of a time series. The point is that the prepend happens **before** the\ndifference rather than after; appending changes the last difference instead.",
  "src": "8450d8d94f69"
 },
 "tensor/Tensor.dist": {
  "en": "`‖x − y‖_p`. An assembly, so the backward follows on its own.\n\n**It did not take `p` for a long time** — it was always L2, and `dist(a, b, 3)` produced a\ndifferent value of plausible magnitude. The core had the same place, and both surfaced only\nthrough comparing values.",
  "src": "62c37d47229f"
 },
 "tensor/Tensor.div": {
  "en": "**The type diverges.** True division is always float, but truncating or flooring **comes back\nas the input's type** (measured: `int64 / int64` with `trunc` is int64). Match only the value\nand leave the type float, and it diverges later wherever indexing wants an integer.",
  "src": "e104539c2998"
 },
 "tensor/Tensor.dot": {
  "en": "The inner product of two vectors.",
  "src": "e538cc129c7b"
 },
 "tensor/Tensor.double": {
  "en": "**Double precision is absent for a different reason.** The eight above are slots we chose not to have;\nthis one is that WebGPU shaders have no `f64` at all. The wording differs accordingly — the Python\nbinding stops with the same sentence.",
  "src": "b30c72a30ad3"
 },
 "tensor/Tensor.dropout": {
  "en": "Picks slots to drop and **scales the survivors by `1/(1-p)`.**\n\nThe scaling is the point — it is what makes the expectation during training match inference,\nand leaving it out makes the two modes differ in magnitude. `p=1` is branched separately:\n`1/(1-p)` becomes a division by zero and produces NaN, and NaN differs even from itself, so no\ncomparison after it passes.\n\n**It drops different slots on every call.** The seed is advanced as it goes — drawn once and\ncached, the same slots die every step, and that is not dropout.",
  "src": "edffd8d45bb3"
 },
 "tensor/Tensor.dropoutSeed": {
  "en": "The seed dropout uses. It rises on every call, so as not to drop the same slots twice.",
  "src": "80ed767eacdc"
 },
 "tensor/Tensor.dtype": {
  "en": "**A label.** The values are always in a float32 buffer — the details are in `src/dtype.ts`.",
  "src": "e748000db151"
 },
 "tensor/Tensor.eigh": {
  "en": "Eigenvalues and eigenvectors of a symmetric matrix. The eigenvalues are ascending.\n\n**It reads one triangle only** — the lower by default. Jacobi looks at the whole matrix, so it is\nmirrored first and then handed over. The details are written down at `mirror` in `linalg.ts`.",
  "src": "b5b47c17fa27"
 },
 "tensor/Tensor.eigvalsh": {
  "en": "The eigenvalues of a symmetric matrix only.",
  "src": "b8dd0208acf6"
 },
 "tensor/Tensor.elu": {
  "en": "The negative side lies down exponentially. **It takes α** — the table's `elu` is baked at α=1.\n\n`nn.ELU(0.5)` is a form that exists in torch, so keeping only the argument-free version stops\nthat line. At α=1 the value equals the table's, so **without varying α the two cannot be told\napart.**",
  "src": "52c497d055ed"
 },
 "tensor/Tensor.empty": {
  "en": "**It leaves the values at 0** — torch gives you garbage, and that is not a thing to learn.",
  "src": "2d2d48ae17fd"
 },
 "tensor/Tensor.emptyLike": {
  "en": "**It borrows the shape only.** Unlike torch it leaves the values at 0 — garbage is not a thing to learn.",
  "src": "7e3631a0794a"
 },
 "tensor/Tensor.equal": {
  "en": "Whether the shape and values are **exactly** equal. There is no tolerance — that is what differs from\n`allclose`.\n\nIt reads back from the GPU. Making a round trip for one verdict is a shame, but holding a CPU copy and\ncomparing that cannot see what happened on the GPU.",
  "src": "727abe2191e1"
 },
 "tensor/Tensor.expand": {
  "en": "Stretches axes of size 1. `-1` means \"leave this one alone\".\n\nAxes can also be added in front — those axes have stride 0, so **nothing is replicated.**",
  "src": "a22e8701341d"
 },
 "tensor/Tensor.exponential_": {
  "en": "The exponential distribution. Its mean is `1/lambd`, and **lambd has to be positive**.",
  "src": "13b963d1e359"
 },
 "tensor/Tensor.eye": {
  "en": "The identity matrix. **Not only square** — torch's `eye(n, m)` makes a rectangle.",
  "src": "2b2512d4411e"
 },
 "tensor/Tensor.fakeQuantizePerChannelAffine": {
  "en": "A different scale per slot. The scale varies along one axis.",
  "src": "9219864dfb8b"
 },
 "tensor/Tensor.fakeQuantizePerTensorAffine": {
  "en": "Imitates quantisation **on floats** — `clamp(round(x/s) + z)` and back.\n\n**No quantised dtype is needed.** The name had it counted as a refusal for a long time, and\nmeasuring showed torch takes a float tensor and produces a float too.\n\n**The gradient is 1 only inside the range** (measured). Rounding is a staircase whose\nderivative is zero almost everywhere, and left as-is no training goes below this layer at all\n— torch leaves that place as a straight-through.",
  "src": "1d79867dadd1"
 },
 "tensor/Tensor.featureDropout": {
  "en": "**Drops channels, not elements.**\n\nSitting next to `dropout`, the name reads as \"the N-dimensional one\", and the work is\ndifferent — a whole channel goes to zero or stays whole. Drawing the mask as `(N, C, 1, …)`\nand broadcasting writes that meaning down directly.",
  "src": "4abf9ddd383c"
 },
 "tensor/Tensor.fillDiagonal_": {
  "en": "The diagonal to one value.\n\n**`wrap` only means something on a tall matrix.** The stride is `cols + 1` either way, and all\nthat differs is **where it stops** — off, it stops at `cols²` slots and paints only the first\nblock's diagonal; on, it runs to the end and paints the lower blocks' diagonals too. numpy's\n`fill_diagonal` is exactly that one line and torch takes it as-is.\n\nIt was first written down as \"the wrapped position skips a row\", and coded that way — no such\nrule exists. There was one case and it asked with a square, so that branch was **passing through\nnothing** — a case asking with a tall matrix goes in alongside.",
  "src": "8fcbc233ac58"
 },
 "tensor/Tensor.fillWith": {
  "en": "A **new** tensor of the same shape filled with one value. **Not in place** — torch's `fill` is\nso, and it does something different from `fill_`, one character away (measured).",
  "src": "8ab449a77cb7"
 },
 "tensor/Tensor.flip": {
  "en": "Reverses one axis.",
  "src": "1edbbf7cefd4"
 },
 "tensor/Tensor.float": {
  "en": "torch's **named type conversions.** The same work as `to(type)` under a different name.\n\nWithout these names, `x.float()` failed in borch.ts alone. Both Python versions had them and the\ngolden line stood as \"a Python-side matter, not carried across\", when in fact it was not that the\nother side could not — it was a name **nobody had asked about.**",
  "src": "6d51cc8f87f8"
 },
 "tensor/Tensor.floatPower": {
  "en": "A power. **torch computes it promoted to double** — we do not have that type.\n\nThe value is the same as `pow` and only the type differs, and having no such type they are the\nsame thing here. That is also why torch itself refuses `float_power_` at a float32 slot — there\nis nowhere to write it back.",
  "src": "1c8347113add"
 },
 "tensor/Tensor.floats": {
  "en": "How many f32 slots the buffer actually holds. For reals it equals `size`; **for complex it is twice that.**\n\nPlaces that only ask \"how many slots of value are there\" — reads, copies, lifetimes — use\nthis. Using `size` directly cuts off the back half of a complex quietly, with the shape and\nthe element count both looking plausible.",
  "src": "b4d92c2fada1"
 },
 "tensor/Tensor.floorDivide": {
  "en": "The quotient along `dim` by **floor division.** It rounds towards −∞, not towards zero.",
  "src": "0982fe8de72d"
 },
 "tensor/Tensor.fmod": {
  "en": "The remainder. **torch's `fmod` follows the sign of the dividend** — unlike Python's `%`.\n\nGradient flows to the dividend as 1. The divisor side is a staircase, so nothing flows.",
  "src": "6cc535e2b860"
 },
 "tensor/Tensor.fold": {
  "en": "Folds the spread back. **Overlapping positions are added** — that is what this function means.",
  "src": "a6659481d7a6"
 },
 "tensor/Tensor.fractionalMaxPool": {
  "en": "Max pooling whose window positions are shaken by a sample.\n\n**The sample differs per plane** — torch's `_random_samples` is `(N, C, axis)`, so the windows\ndiverge per plane. Hence it loops over the planes. Expensive, and the same as torch.\n\n**The axis order differs by dimensionality.** ATen's 2-D version reads the sample as (width,\nheight) and the 3-D version as (depth, height, width) — the two functions are out of step with\neach other, and what is imitated here is that inconsistency.",
  "src": "0a9e7ab3728a"
 },
 "tensor/Tensor.frexp": {
  "en": "The mantissa and the exponent. `x = mantissa × 2^exponent`, with the mantissa in [0.5, 1).\n\ntorch returns the exponent as int32, and storage here is f32 only, so it matches by value.",
  "src": "bbed26b82d6e"
 },
 "tensor/Tensor.frombuffer": {
  "en": "Reads the bytes **as they are.** `offset` is in bytes, not elements.\n\n`dtype` here decides **what to read them as.** That differs from converting after reading —\nthe same bytes become very different numbers depending on the type, so fixing it later with\n`to()` comes after they have already been read as something else.\n\n`complex64` is refused. The storage is interleaved so the bytes could be laid on directly,\nbut that arrangement is not nailed down as a promise, which would leave **the reader not\nknowing what they received** — better not to offer the name.",
  "src": "c81a9bb861d5"
 },
 "tensor/Tensor.gather": {
  "en": "Selects along one axis as an index tensor points. The indices arrive held in float32.",
  "src": "3f08541e85a7"
 },
 "tensor/Tensor.gatherSpots": {
  "en": "Reads the slots the index table points at and produces `shape`.\n\n**Overlapping indices accumulate gradient** — a slot read twice receives twice. Measured with\nnon-overlapping steps alone, that accumulation never shows.",
  "src": "b7ad40d00691"
 },
 "tensor/Tensor.gaussianNllLoss": {
  "en": "The Gaussian negative log-likelihood.\n\n**The variance is clamped by `eps`.** Unclamped it divides by zero and goes to infinity.",
  "src": "8624cc0fe447"
 },
 "tensor/Tensor.geluTanh": {
  "en": "The `approximate=\"tanh\"` GELU. The table's `gelu` is **the exact form** (erf).\n\nThe maximum difference is around 1e-4, so it is a place where \"near enough, keep one\" almost\ngot through. torch keeps them apart because the tanh one is faster, not because they are the\nsame.",
  "src": "3db226cad239"
 },
 "tensor/Tensor.geometric_": {
  "en": "The geometric distribution. **Being discrete, it runs on integer tensors too** — that is what separates it from the continuous ones.\n\n`p` is an **open interval**. At 0 the event never happens and at 1 it always happens on the first try, so neither is a distribution.",
  "src": "b76cea48d433"
 },
 "tensor/Tensor.geqrf": {
  "en": "Produces QR **in reflector form.** `householderProduct` stands `Q` up from it.\n\n**With everything below the diagonal already zero it does not reflect** — LAPACK's `dlarfg` puts\n`tau = 0` there and leaves the values alone. The last column of a square matrix is always that\ncase.",
  "src": "f63afb188ada"
 },
 "tensor/Tensor.ger": {
  "en": "The old name for the outer product. The same as `outer`.",
  "src": "e32439226976"
 },
 "tensor/Tensor.glu": {
  "en": "Splits the axis in half and gives `a · σ(b)`. The only activation that is not elementwise.",
  "src": "7043ed3e0651"
 },
 "tensor/Tensor.gradient": {
  "en": "The central difference. **One per axis, returned as a bundle** — no axis given, it is all of them.\n\nAt `edgeOrder` 1 the ends use a one-sided difference; at 2 they are fitted with a quadratic\n(measured: on `x²`, 2 gives the exact derivative and 1 is off at both ends).",
  "src": "bd4fab811d35"
 },
 "tensor/Tensor.groupNorm": {
  "en": "Normalises with the channels bundled into groups.\n\nIt differs from `layerNorm` **only in the span it folds.** With one group the whole channel set\nis one bundle, which is LayerNorm; with as many groups as channels it is per channel, which is\nInstanceNorm — the three are special cases of one another, and a wrong bundling rule makes two\nof the three identical.\n\nThe bundled span is laid down onto a single last axis and folded. That way `layerNorm`'s formula\nis used directly, and the normalisation formula lives in one place.",
  "src": "9c6bda3710bd"
 },
 "tensor/Tensor.half": {
  "en": "**Types that do not exist.** The names are kept for the message — with no name at all you get\n`'half' does not exist`, and that **cannot be told apart from a typo.** Both Python versions stop with\nthe same sentence, so this one matches. It is a place matching text rather than values, so comparing\nthem against each other catches nothing, and a learner reading divergent wording reads it as\n**differing per implementation.**",
  "src": "1e67942e19f3"
 },
 "tensor/Tensor.hammingWindow": {
  "en": "`alpha - beta·cos`. torch's defaults are 0.54/0.46.",
  "src": "dc124c723fcb"
 },
 "tensor/Tensor.hardshrink": {
  "en": "|x| > λ passes through, otherwise 0. **The boundary goes to the zero side** (`>`, not `>=`).",
  "src": "cd63142638f4"
 },
 "tensor/Tensor.hingeEmbeddingLoss": {
  "en": "`x` itself at `y=1`, and `max(0, margin − x)` at `y=−1`.\n\n**It is not a split between the two but a sum of them.** torch puts the margin term where\n`y ≠ 1` and `x` where `y ≠ −1` and **adds them** — at ±1 only one is on and it matches the usual\nformula, but at `y=0` **both** are on. Split on `y > 0` it diverged quietly there, and `sign()`\ndoes produce 0.",
  "src": "70c2e8f056ac"
 },
 "tensor/Tensor.histc": {
  "en": "How many in each bucket. **With `min === max` it uses the data's own range** (measured).\n\nGiven a range it **discards what falls outside** — it does not pile them into the end buckets.\nMeasured with data entirely inside the range, that rule never surfaces.",
  "src": "8b933b8e2635"
 },
 "tensor/Tensor.histogram": {
  "en": "The same count as `histc`, **plus the boundaries.**\n\nGiven a tensor for `bins`, that is the boundaries — the bucket widths may differ, and then\n`density` divides by a different value per bucket.",
  "src": "19596e198c6a"
 },
 "tensor/Tensor.histogramdd": {
  "en": "A histogram over several axes. `this` is `(samples, dimensions)`.",
  "src": "8735a6917d08"
 },
 "tensor/Tensor.householderProduct": {
  "en": "Multiplies the reflectors to stand `Q` up. The counterpart of `geqrf`.\n\n`v_i` has 1 on the diagonal and `A[i+1:, i]` below it — the 1 on the diagonal is **a promise not to\nstore it**, so reading that slot mistakes the `R` the factorisation put there for a reflector.",
  "src": "5d90b0bd47b3"
 },
 "tensor/Tensor.hstack": {
  "en": "One-dimensional inputs are joined end to end; above that, **column-wise.**",
  "src": "469f50037bf8"
 },
 "tensor/Tensor.huberLoss": {
  "en": "**It equals `smoothL1Loss` only at δ=1.**\n\nThe real relation is `huber(δ) = δ · smoothL1(β=δ)`. Measured at the defaults alone, treating\nthem as one function passes, so the golden cases ask with δ varied.",
  "src": "a1d2ddf411f9"
 },
 "tensor/Tensor.imag": {
  "en": "The imaginary part. **The gradient is `0 + gi`** — written as `−gi` it runs plausibly with only\nthe sign flipped. Measuring torch, the gradient of `z.imag` is `0+1j`.",
  "src": "115234b2c427"
 },
 "tensor/Tensor.indexPut": {
  "en": "Takes one index tensor per axis and writes at those positions.\n\nThe per-axis indices are **folded into one flat index** — which makes it the same door as `put`.\nThe folding is tensor arithmetic, so no kernel is added.",
  "src": "39847eb80390"
 },
 "tensor/Tensor.indexReduce": {
  "en": "Combines the **rows** the indices point at. Unlike `scatterReduce`, the indices are per row.",
  "src": "83e564d2e0d6"
 },
 "tensor/Tensor.indexSelect": {
  "en": "An index **vector** selects along one axis. Unlike `gather`, the index does not differ per slot.",
  "src": "c1358a70f48b"
 },
 "tensor/Tensor.inner": {
  "en": "Folds and multiplies **along the last axes.** One-dimensional it is the inner product; above that it is `a @ bᵀ`.\n\nTwo dimensions is where it parts from `dot` — `dot` takes one dimension only. This name was\nmissing for a long time without surfacing, because the check sweeping the source with a regular\nexpression was counting the local variable `const inner = …` as a public name.",
  "src": "022a1cd0e223"
 },
 "tensor/Tensor.inplaceUnary": {
  "en": "The table's unary operations, in place. Names like `abs_` come here.",
  "src": "45ccf7b8ebcb"
 },
 "tensor/Tensor.instanceNorm": {
  "en": "Per sample and per channel. `groupNorm` with the group count set to the channel count.",
  "src": "88ed90b30f79"
 },
 "tensor/Tensor.interpolate": {
  "en": "Enlargement taking `size`, `scaleFactor` and `mode` in one place. The `Upsample` layer is this.\n\n**Taking `mode` and then not using it is the reason this function exists.** Nearest and bilinear\nare entirely different kernels, and without dispatching on it, asking for bilinear gives you\nnearest — not an exception, a quietly different value.",
  "src": "0e14d6188232"
 },
 "tensor/Tensor.interpolateBilinear": {
  "en": "Bilinear enlargement.\n\n**`alignCorners` changes the values.** True pins both ends and divides evenly between them;\nfalse measures from the centre of a cell. `UpsamplingBilinear2d` is true and\n`Upsample(mode='bilinear')` defaults to false, so treating them as aliases on the strength of\nthe names misaligns the edges — the interior is close enough that an eye does not separate\nthem.",
  "src": "ecda87118bb2"
 },
 "tensor/Tensor.invEx": {
  "en": "The same as `inv` except that it **does not throw** — it puts a non-zero number in `info` instead.",
  "src": "03cc14266bd5"
 },
 "tensor/Tensor.inverse": {
  "en": "The inverse. The backward is `-A⁻ᵀ·Ḡ·A⁻ᵀ`.",
  "src": "2196be0618b5"
 },
 "tensor/Tensor.isComplex": {
  "en": "Whether this tensor is complex. Where `torch.is_complex` goes.",
  "src": "48a28a1d87bd"
 },
 "tensor/Tensor.isConj": {
  "en": "Whether the conjugate bit is raised. **Here it is always false.**\n\ntorch's `conj()` does not change the values and **raises one bit** — the actual conjugate is made\nlater when needed (hence the separate `resolve_conj`). `conj` here makes the conjugate on the spot,\nso there is nothing to defer and no bit to raise.\n\n**The name is kept anyway.** Without it, `x.isConj()` stops with \"no such thing\", and that question\ndoes have an answer — false. Having no lazy bit is an implementation's business, not a reason for\nthe question to lose its meaning.",
  "src": "1d0efe0828e6"
 },
 "tensor/Tensor.isFloatingPoint": {
  "en": "Whether the type is a floating-point one. **It asks about the type, not the values** — it can be\ntrue with every value an integer.\n\nLooking at `dtype` would tell you, but the name exists in torch and did not exist here. The\nbinding was writing that branch in Python, which leaves nowhere for the TypeScript side to ask.",
  "src": "fefc4d442752"
 },
 "tensor/Tensor.isNeg": {
  "en": "Whether the sign bit is raised. Always false, for the same reason as `isConj`.",
  "src": "483d85dd2073"
 },
 "tensor/Tensor.isNonzero": {
  "en": "Whether this single-element tensor's value is non-zero.\n\n**With more than one it stops.** It is the place that keeps Python's `if tensor:` from quietly\nlooking at the first element, which is why torch gives this name separately. It has to look at the\nvalue, so it is asynchronous.",
  "src": "0541f65f6808"
 },
 "tensor/Tensor.isSigned": {
  "en": "Whether the type can hold negatives. Only `bool` is false.",
  "src": "2898441e306a"
 },
 "tensor/Tensor.isclose": {
  "en": "Whether each position is **close.** `|a−b| ≤ atol + rtol·|b|`.\n\n**It leans to the right** — it measures against `b`'s magnitude, so swapping the two can change\nthe answer. torch defines it that way.",
  "src": "72701eadc32d"
 },
 "tensor/Tensor.isin": {
  "en": "Whether each element is in that list. It falls out of broadcasting alone — no values are read.",
  "src": "3d8c24bb6d06"
 },
 "tensor/Tensor.isreal": {
  "en": "Whether there is only a real part. **Non-complex, everything is true, and that is a fact.**",
  "src": "b0dd4ae3bb79"
 },
 "tensor/Tensor.kaiserWindow": {
  "en": "`I₀(β√(1-((k-h)/h)²)) / I₀(β)`. torch's default `beta` is 12.0.",
  "src": "850633add285"
 },
 "tensor/Tensor.klDiv": {
  "en": "`target · (log target − this)`. **This side must already be in logs.**\n\n**There are four reductions.** `mean` divides by the element count and `batchmean` by the batch\nsize — the latter is the one that matches the mathematical definition, and torch itself warns\nthat it intends to change.",
  "src": "90a0dfdb0454"
 },
 "tensor/Tensor.kron": {
  "en": "The Kronecker product. **One-dimensional only** — above that it refuses.\n\nThe version in the binding was looking at one axis only (two `shape[0]`s), and given two\ndimensions the answer was quietly wrong. It could have been fixed while carrying it across, but\nthat writes down a capability that is not here as though it were — **a missing feature beats a\nwrong answer** is this repository's rule.",
  "src": "3358d1ca0af5"
 },
 "tensor/Tensor.kthvalue": {
  "en": "The `k`-th smallest value. **Counting from 1**, as in torch.",
  "src": "f54509021099"
 },
 "tensor/Tensor.l1Loss": {
  "en": "Absolute error.\n\n**`reduction` was missing here.** Thirteen rarely-used losses all took it and the four most\nused did not — the ones written later followed torch's signature and the ones written first\nwere never fixed. The tutorials use the default, so the table never asked either.",
  "src": "3a47be29b004"
 },
 "tensor/Tensor.layerNorm": {
  "en": "Trailing axes to mean 0, variance 1. **The variance is the biased estimate (divided by n)** —\nthat is torch's `layer_norm`, and it differs from `var()`'s default.",
  "src": "c8d30a977acf"
 },
 "tensor/Tensor.layerNormOver": {
  "en": "Folds the **last `dims` axes together.** `layerNorm` folds one axis only.\n\ntorch's `LayerNorm(normalized_shape)` decides **how many** axes to fold from the length of that\nshape, and measured with a single axis (`LayerNorm(4)`) the two give the same answer — which is\nwhy it went unseen for a long time. Given `(3, 4)` they diverge.",
  "src": "43a8928f47fa"
 },
 "tensor/Tensor.ldlFactor": {
  "en": "A symmetric matrix as `L D Lᵀ`. **It does not pivot.**\n\ntorch uses LAPACK's Bunch–Kaufman, which swaps positions when it has to. Here only the cases with\nnothing to swap (positive definite and the like) are handled, and a near-zero diagonal is refused\nloudly — carrying on quietly gives a different answer with and without swapping, and both are\nplausible.\n\nThe answer is **packed into one plate** in torch's shape — the diagonal is `D` and below it is\n`L`.",
  "src": "8afd3481b77f"
 },
 "tensor/Tensor.ldlFactorEx": {
  "en": "`ldlFactor` **plus one `info`.** The same place as `luFactorEx`.\n\n**Here it is always 0** — meeting a bad position makes `ldlFactor` refuse on the spot, so nothing\nis left to report by number. The name is kept anyway: torch offers it, and without it the caller\nhas to invent one of the three slots themselves — which the binding was actually doing (standing\nup `_Fields` by hand and slotting a 0 into `info`). Then the golden cases go through the binding\nand are green, and what is missing is only for whoever writes TypeScript.",
  "src": "6df8cd424c2e"
 },
 "tensor/Tensor.ldlSolve": {
  "en": "Solves using the factorisation `ldlFactor` produced. Three passes: `L y = b`, `D z = y`, `Lᵀ x = z`.",
  "src": "14c99d875223"
 },
 "tensor/Tensor.leakyRelu": {
  "en": "A gentle slope on the left.\n\nWritten as `max(x, slope·x)` it was **wrong exactly where x is 0.** There the two terms tie\nand `maximum` splits a tie in half, giving (1 + slope)/2, while torch gives the slope — it\nsplits on `x > 0` alone and has no notion of a tie.",
  "src": "15d5bbe0cbb9"
 },
 "tensor/Tensor.lerp": {
  "en": "The position `weight` of the way from `this` to `end`.\n\n**The weight may be a tensor.** `lerpFrom` takes numbers only, so it cannot give a different\nweight per position — torch can.",
  "src": "46cfd6e7c65f"
 },
 "tensor/Tensor.lerpFrom": {
  "en": "`this ← (1-t)·this + t·other`. Running statistics use it.",
  "src": "8c9b94b5c8c9"
 },
 "tensor/Tensor.linear": {
  "en": "`x @ Wᵀ`. torch's `F.linear` takes the weight as (out, in).",
  "src": "583783a19316"
 },
 "tensor/Tensor.linspace": {
  "en": "Evenly divided values, both ends included.",
  "src": "a3f24c5a391e"
 },
 "tensor/Tensor.lobpcg": {
  "en": "The **`k` extreme eigenpairs** of a symmetric matrix.\n\n**torch is iterative and we are exact.** That side iterates to get a few cheaply out of a large\nsparse matrix; we have no sparse and the sizes are small. Measuring showed torch's answer\nconverges to within 7e-6 of the exact one and wobbles by about that much with the seed (measured)\n— far below tolerance. The value is the same and only the cost differs.\n\n**`largest` decides the order too** — true gives largest first, false smallest first (measured).",
  "src": "0bf366526fa6"
 },
 "tensor/Tensor.localResponseNorm": {
  "en": "Divides by neighbouring channels.\n\n**The window is off-centre.** Channel `c`'s window is `[c − n//2, c + n − 1 − n//2]`, so at\n`size=2` it is `{c−1, c}` — centring it shifts the values by one slot, and the size being\nthe same means the shape does not show it.",
  "src": "8f0a809150d6"
 },
 "tensor/Tensor.logNormal_": {
  "en": "The log-normal distribution. `mean` and `std` are the values **after taking logs** (as in torch).",
  "src": "052140a1ef13"
 },
 "tensor/Tensor.logSoftmax": {
  "en": "`log(softmax(x))`. **It does not compute `softmax` and take the log** — small probabilities\nbecome 0 and the log becomes -inf. Written directly as a subtraction, that place does not\nexist.",
  "src": "cb99797f8dde"
 },
 "tensor/Tensor.logcumsumexp": {
  "en": "A cumulative `logsumexp`. Counted **without overflowing** — the axis maximum is subtracted,\nadded, and restored.\n\nAssembled from the same place as `logsumexp`. With the maximum detached, the backward falls\nstraight out of the assembly, and one hand-written derivative is gone.",
  "src": "0e42006534f9"
 },
 "tensor/Tensor.logdet": {
  "en": "`log|det|`. The backward is `A⁻ᵀ` — more stable, with no determinant multiplied in.",
  "src": "3731c649dd6d"
 },
 "tensor/Tensor.logicalXor": {
  "en": "Exclusive or. **Absent from the binary table**, so it is built by asking twice whether it differs from zero.",
  "src": "2e3cfa094fe0"
 },
 "tensor/Tensor.logspace": {
  "en": "Evenly spaced in powers of `base`. It uses `linspace` as the exponent.",
  "src": "1b0edfc2eb0b"
 },
 "tensor/Tensor.logsumexp": {
  "en": "`log(Σ exp(x))`. **Computed with the maximum subtracted** — used plainly, the moment x passes\n89 float32's exp becomes inf and everything after it is inf.\n\nLeft as an assembly. The backward falls out of the derivatives of operations that already\nexist, so no new derivative is written by hand — that was the most frequently wrong place\nthis week.",
  "src": "b41a6c5a0dd8"
 },
 "tensor/Tensor.lpPool": {
  "en": "The `p`-th root of the sum of `p`-th powers.\n\n**It follows torch's assembly exactly** — average pooling, multiplied by the window size to get\nback to a sum, then the root. The sign handling and the `relu` in the middle are that\nimplementation's too.",
  "src": "4da9a274a6d5"
 },
 "tensor/Tensor.lstsq": {
  "en": "The least-squares solution. Square and invertible, it gives the same answer as `solve`.",
  "src": "3370795c2915"
 },
 "tensor/Tensor.lu": {
  "en": "Spread into `P`, `L` and `U`. Easier to read than the packed plate.",
  "src": "fdac66027fbd"
 },
 "tensor/Tensor.luFactor": {
  "en": "`L` and `U` packed into one plate, plus a pivot table **counting from 1.**",
  "src": "ce35c1d2749f"
 },
 "tensor/Tensor.luFactorEx": {
  "en": "`luFactor` **plus one `info`.** Instead of throwing, it reports by number — 0 means it went well,\nand `k` means the `k`-th pivot is zero and the matrix is singular (counting from 1).",
  "src": "9bc830b6814c"
 },
 "tensor/Tensor.luSolve": {
  "en": "`torch.Tensor.lu_solve` — **the receiver is the right-hand side `b`.**\n\ntorch has `b.lu_solve(LU, piv)`, so it is the same here. The side that takes the factorisation as\nreceiver is `luSolveFactored`, which is `linalg.lu_solve`'s argument order.",
  "src": "f1f7aa2c23b0"
 },
 "tensor/Tensor.luSolveFactored": {
  "en": "Solves `A x = b` with the factorisation. **`this` is the LU** — the argument order of\n`linalg.lu_solve(LU, pivots, B)`, and a different receiver from torch's `Tensor.lu_solve`.\n\nSo **the torch claim was taken out of the name.** This used to be `luSolve`, and someone carrying\ntorch across writing `b.lu_solve(LU, piv)` puts `LU` into the `pivots` slot — the name and the\nargument count both match, so **nothing catches at that point** and only the value is wrong.",
  "src": "438d1df09c54"
 },
 "tensor/Tensor.luSolveTop": {
  "en": "The old name. Kept because the binding calls it — one line of delegation.\n\nIt was made to point at the opposite side back when `luSolve` took the factorisation as receiver,\nand now that `luSolve` itself sits where torch does, **there is no opposite side for `Top` to point\nat.** Once the binding moves to `luSolve` it can be deleted.",
  "src": "4b6507954ff3"
 },
 "tensor/Tensor.luTop": {
  "en": "`(LU, pivots)`. **It produces something different from `lu()`** — that one spreads into `P·L·U`\nand this one gives **one packed plate and a swap list** (measured).\n\nWith `getInfos` an info code is attached third. Ours is always 0 — meeting a singular matrix it\nthrows on the spot rather than reporting quietly by code.",
  "src": "51a67c5cfdca"
 },
 "tensor/Tensor.luUnpack": {
  "en": "Spreads one packed plate into `P·L·U`.\n\n**Turned off, what comes back is an empty tensor rather than `null`** (measured: its shape is\n`[0]`). Returning `null` makes the receiving side branch on it, and that is not torch code.",
  "src": "467bc19c54f7"
 },
 "tensor/Tensor.marginRankingLoss": {
  "en": "`max(0, −y·(x₁ − x₂) + margin)`.",
  "src": "8118f8d03a92"
 },
 "tensor/Tensor.maskedFill": {
  "en": "Overwrites the true positions with a value. **No gradient goes to an overwritten slot.**",
  "src": "0da88ee02d24"
 },
 "tensor/Tensor.maskedScatter": {
  "en": "Fills `source` **in flat order** into the positions where the mask is true.\n\nWhich value goes where depends on the mask's **values** while the result shape is fixed — so\nrather than reading the values back, it is solved with a kernel that counts, per position, how\nmany trues came before.",
  "src": "a5b2156731f0"
 },
 "tensor/Tensor.maskedSelect": {
  "en": "Picks the true positions into one dimension. **Gradient flows to the picked positions.**",
  "src": "32b23d6dfdde"
 },
 "tensor/Tensor.matrixExp": {
  "en": "The matrix exponential `e^A`. **It has a gradient.**\n\nThe backward uses the table `matrixExpAdjointMap` in `linalg.ts` froze during the forward — why a\ntable is written down there (briefly: `Ḡ` is on the GPU and `expm` is CPU). **It is built only\nwhen the gradient is wanted.** One table is `n²` calls to `expm`, which is not free.",
  "src": "2851924d0a28"
 },
 "tensor/Tensor.matrixNorm": {
  "en": "The norm measured treating it as a matrix. **A different number per branch.**\n\nThe default is Frobenius; `2` is the largest singular value; `nuc` the sum of singular values; `1`\nthe maximum column absolute sum; and `inf` the row equivalent. Only the three that need singular\nvalues make a CPU round trip.",
  "src": "59ab763b91a4"
 },
 "tensor/Tensor.matrixPower": {
  "en": "An integer power. For now it repeats the multiplication — for small exponents only.",
  "src": "2c3f62322d3c"
 },
 "tensor/Tensor.max": {
  "en": "The value and the index together. torch's `x.max(dim)`.\n\nCalling `amax` and `argmax` separately does work, but this is the shape torch code uses, and\n**whether the two point at the same place** is confirmed only here. `amax` splits a tie evenly\nand `argmax` picks the first single position, and re-fetching the value by index does no\nsplitting — torch's `max(dim)` is the latter.",
  "src": "f74a71ed6199"
 },
 "tensor/Tensor.maxPool2d": {
  "en": "The maximum of non-overlapping windows. `this` is `(N, C, H, W)`.",
  "src": "0114f3ca0b91"
 },
 "tensor/Tensor.maxUnpool": {
  "en": "Places values at the slots the indices point at and leaves the rest zero.\n\nWhat pooling discarded cannot be brought back, so the output size is given from outside. The\ndefault is `(n-1)·stride - 2·padding + kernel`, and `outSize` can name it directly.",
  "src": "e2158993d534"
 },
 "tensor/Tensor.median": {
  "en": "The median. **With an even count it gives the lower** — torch does not average the two.\n\n**One NaN anywhere makes it NaN** (measured). Sorting pushes NaN to one end, so simply picking\n**skips the NaN** and returns an intact value — that is `nanmedian`, and this is not. The core had\nthe same defect, and adding a case that asks the two side by side caught both at once.",
  "src": "c763757c14e3"
 },
 "tensor/Tensor.meshgrid": {
  "en": "A grid. **`xy` has the first two axes swapped**, so one rule cannot cover both.\n\nMeasured only with `ij`, that branch is invisible — on square input the two have the same\nshape, and only the values separate them.",
  "src": "d1c98bb10972"
 },
 "tensor/Tensor.mm": {
  "en": "Two-dimensional only. Batched matrix multiply is T1 — a missing feature beats a wrong answer.",
  "src": "6b14fa96a47b"
 },
 "tensor/Tensor.mode": {
  "en": "The most frequent value. **On an equal count the smaller value wins, and the index is that\nvalue's last occurrence** (measured: `[4,4,5,5]` gives value 4 at index 1).\n\nMeasured with data that has no ties, that rule never surfaces.",
  "src": "e717bf38629e"
 },
 "tensor/Tensor.movedim": {
  "en": "Moves an axis to a chosen position. Unlike `swapaxes`, it preserves the order of the rest.",
  "src": "9fd460b19e79"
 },
 "tensor/Tensor.mseLoss": {
  "en": "Squared error.",
  "src": "d95161124bc3"
 },
 "tensor/Tensor.msort": {
  "en": "Sorts only; it does not give positions.",
  "src": "975700250ea8"
 },
 "tensor/Tensor.multiMarginLoss": {
  "en": "The margin between the target position and the rest.\n\n**It divides by the number of classes**, not the number of pairs compared. That means the target\nposition is in the denominator too, and dividing by the pair count gives 3/2 times too much with\nthree classes.",
  "src": "6c6b2c02f206"
 },
 "tensor/Tensor.multilabelMarginLoss": {
  "en": "**The target is a list of positions, and −1 marks the end.**\n\n`[3, 0, -1, 1]` means \"3 and 0 are correct\", and the trailing 1 is not read. Break that\nconvention and you either count −1 as one of the classes or keep reading past the end.\n\nHow far to read is decided by **a cumulative sum** — everything after a `−1` is switched off.\nScattering the target positions as 0/1 after that means never reading back to the CPU. The\nreason it scatters by **accumulating** rather than overwriting is that a slot past the end,\nclamped, could otherwise erase a 1 in front of it.",
  "src": "0a31df8e85e1"
 },
 "tensor/Tensor.multilabelSoftMarginLoss": {
  "en": "Independent binary classification per slot, **averaged over the whole class set.**",
  "src": "72fc04fd05d2"
 },
 "tensor/Tensor.mv": {
  "en": "Matrix × vector. The work `mm` does, but torch gives it a name of its own.\n\nIt stands the vector up as a single column and then removes that axis again — the result has to be\none-dimensional.",
  "src": "26be9d6121d9"
 },
 "tensor/Tensor.mvlgamma": {
  "en": "The multivariate log-gamma. `log Γ_p(x) = p(p−1)/4 · log π + Σᵢ log Γ(x + (1−i)/2)`.\n\nAt `p` of 1 it equals `lgamma` — asked with that value alone, whether the sum runs is\ninvisible.",
  "src": "38ae6ec23a91"
 },
 "tensor/Tensor.nanToNum": {
  "en": "NaN and infinities to finite numbers. **Unspecified, they are f32's extremes** (as in torch).\n\nIt selects with `where` — multiplying by a mask gives `0 × NaN = NaN`, which contaminates the very\nslots that were filtered out. A place this repository has been bitten by three times.",
  "src": "67a9b6ad71b4"
 },
 "tensor/Tensor.nanmean": {
  "en": "Averages excluding NaN. **The count excludes NaN too** — that is what differs from mean.",
  "src": "cbd2435fb8ad"
 },
 "tensor/Tensor.nanmedian": {
  "en": "The median counted **excluding** NaN. `median` returns NaN if there is even one (measured).\n\nWith an even count it **takes the lower** — it does not average.",
  "src": "3b7406b2a89b"
 },
 "tensor/Tensor.nanquantile": {
  "en": "Quantiles counted excluding NaN.\n\n**Removing NaN shifts the positions.** So a small tensor of the intact slots is built and the\nwork is retraced on that — the way back to the original positions is `indexSelect`, which is\nalready differentiable, so it stays connected.",
  "src": "14c72251486a"
 },
 "tensor/Tensor.nansum": {
  "en": "Adds, treating NaN as 0. No gradient goes to a NaN slot.",
  "src": "9bae327410b3"
 },
 "tensor/Tensor.narrow": {
  "en": "Only `length` entries from `start` along one axis.",
  "src": "7e2cbd305a92"
 },
 "tensor/Tensor.nllLoss": {
  "en": "The negative log-likelihood. `this` must already have been through `log_softmax`.\n\n**The graph breaks easily where the target position is extracted.** Return the value detached\nand no gradient reaches the extracted slot, making the whole classification loss\nnon-differentiable.",
  "src": "f6e4bc6fc6a1"
 },
 "tensor/Tensor.node": {
  "en": "The public door onto `make`. **Another module with kernels of its own** (`fft.ts`) enters here.\n\n`make` itself is not exported because the name is too common — seen from outside,\n`Tensor.make` does not read as making anything in particular. `makeNode` at the end of the\nfile wraps this.",
  "src": "8c860d89687f"
 },
 "tensor/Tensor.nonzero": {
  "en": "The coordinates of the non-zeros. Positions are not values, so there is no gradient.",
  "src": "f58bc6bb3227"
 },
 "tensor/Tensor.nonzeroStatic": {
  "en": "The non-zero positions, **a fixed number of them.** Short, it pads; over, it cuts.\n\n`nonzero`'s result size depends on the values and so needs one read off the GPU, whereas this\none is given the size up front and **in principle** needs no such round trip — that is the name's\nwhole reason. Here it still reads (which positions are non-zero is a value). A place to move\ninto a kernel.",
  "src": "ab0aa983fcd3"
 },
 "tensor/Tensor.norm": {
  "en": "The L2 norm.",
  "src": "e1d93dbc533d"
 },
 "tensor/Tensor.normal": {
  "en": "A sample from the normal distribution. **With `std` at 0 it is the mean itself** (measured) —\nthat endpoint is the only place that asks whether this name really looks at the standard\ndeviation.\n\ntorch offers two forms: two tensors give a per-slot mean and standard deviation, while two\nnumbers and a shape give the same one everywhere. The first **broadcasts** — a `(2,)` mean\nwith a scalar standard deviation is allowed.",
  "src": "2dc1304b507e"
 },
 "tensor/Tensor.normalize": {
  "en": "Length to 1 along an axis. `eps` stops the division blowing up on a zero vector.",
  "src": "0e3824ffed95"
 },
 "tensor/Tensor.oneHot": {
  "en": "An index as a position marker. One of `n` slots, 1 at its own.",
  "src": "477b689d3a4e"
 },
 "tensor/Tensor.oneHotAlong": {
  "en": "A table with 1 only at the largest position along one axis.\n\n**A tie must not be settled with `eq`** — two equal values give two ones, and then\n`gumbelSoftmax(hard=true)`'s answer is not one-hot. It almost never happens with random\nnumbers, but \"almost\" is not a guarantee. It picks the index and places at that slot alone.",
  "src": "57d92ee4d277"
 },
 "tensor/Tensor.orgqr": {
  "en": "Another name for `householderProduct`. torch offers both.",
  "src": "64f5ddbd5b61"
 },
 "tensor/Tensor.ormqr": {
  "en": "Multiplies into `C` **without standing Q up** — here it stands it up and multiplies. The value is\nthe same and nothing is saved at this size.\n\n**It is a different Q from `orgqr`'s.** That one gives a Q **cut** to `m×k`, while this uses the\nuncut `m×m` — the reflectors are maps on `Rᵐ`, and cutting multiplies by only part of that map.\nThe answer diverges entirely on a tall matrix (measured). Measured on squares alone, the two are\nthe same and nothing shows.",
  "src": "925674deb14a"
 },
 "tensor/Tensor.outer": {
  "en": "The outer product of two vectors. It falls out of broadcasting — no new kernel needed.",
  "src": "c510d05a62a6"
 },
 "tensor/Tensor.owned": {
  "en": "Filled with a value but **without the cache — it gets its own buffer.**\n\n**Anything that will be modified in place has to come this way.** `full` caches\nsingle-element tensors by value, so calling `zeros([1])` twice returns **the same buffer.**\nThat in itself is right — constants are only read. The problem is something that is not a\nconstant passing through that door:\n\n- `Adam`'s m and v become the same buffer at a size-1 parameter, and WebGPU invalidates\n  **the whole command buffer** for \"writable storage buffer aliasing\"\n- `SGD`'s momentum buffer overwrites the zero constant the entire program uses\n- `nn.PReLU()` defaults to a single parameter, so its weight *is* the global 0.25 constant,\n  and the optimizer edits it during training\n- `BatchNorm(1)`'s running statistics overwrite the global 0 and 1 constants\n\nOnly the first throws; the rest are **quietly wrong.** So parameters, optimizer state and\nrunning statistics come here whatever their value.",
  "src": "de2c86ea39cc"
 },
 "tensor/Tensor.pad": {
  "en": "Pads a constant before and after one axis. For several axes, call it per axis.",
  "src": "9c5941dccc1c"
 },
 "tensor/Tensor.padND": {
  "en": "Where `F.pad` goes — taken **from the last axis, in (before, after) order.**\n\n**The pair count and the rank interlock.** One pair needs rank 2 or 3, two pairs rank 3 or 4,\nthree pairs rank 4 or 5 — torch refuses anything else. Accepting any rank lets a wrong axis\npass.\n\nConstant goes to the existing kernel because that is shorter; the other three go through\nindexing.",
  "src": "5f363c956965"
 },
 "tensor/Tensor.padSequence": {
  "en": "Puts things of differing lengths into one batch.\n\nThe default is `(longest length, count, …)`, and with `batchFirst` the first two swap.\n**Gradient returns to each piece** — the padded region is zero, so `pad`'s backward trims it\naway.",
  "src": "b03f37779ac8"
 },
 "tensor/Tensor.pairwiseDistance": {
  "en": "The distance between two paired rows.\n\n**`eps` is added to the difference, not to the result.** Asked at `p=1` where the difference is\nexactly 1.0, it gives 1.0000020 (= 1 + 2·1e-6) — read as added to the result it would be\n1.000001, one digit out. Confirmed by measurement.",
  "src": "9e32ff5cb513"
 },
 "tensor/Tensor.parents": {
  "en": "Upstream in the graph. **Only `detach_` changes this** — hence not `readonly`. Edited\nanywhere else, the backward of an already-made node changes quietly.",
  "src": "64632e6beb7b"
 },
 "tensor/Tensor.pcaLowrank": {
  "en": "Low-rank PCA. **With `center=false` it is the same thing as `svdLowrank`** (measured).\n\nCentring is the whole difference between this function and that one. Measured at true alone, that\nbranch never surfaces.",
  "src": "0d459eeb1b54"
 },
 "tensor/Tensor.pdist": {
  "en": "The distance between **every pair** within one bundle. It gives the upper triangle only.",
  "src": "6b24321d3062"
 },
 "tensor/Tensor.permute": {
  "en": "Reorders the axes wholesale.",
  "src": "ceba429cac28"
 },
 "tensor/Tensor.pinverse": {
  "en": "The pseudoinverse. **It has a gradient** — in three terms.\n\n    Ā = −Pᵀ·Ḡ·Pᵀ + (I − A·P)·Ḡᵀ·P·Pᵀ + Pᵀ·P·Ḡᵀ·(I − P·A)\n\nThe last two terms **go to zero for a square invertible matrix.** There `I − AP` and `I − PA` are\nboth zero, only the first term remains, and that first term is the same formula as the inverse's\ngradient. So leaving the two out is right on squares and wrong only on rectangles — the golden\ncases ask with rectangles too.",
  "src": "965b73c5ed4f"
 },
 "tensor/Tensor.pixelShuffle": {
  "en": "`(N, C·r², H, W)` → `(N, C, H·r, W·r)`. Cuts channels and plants them in space.\n\n**The interleaving order is the whole of the value.** The channels split into `(C, r, r)` and\nthe two `r`s are inserted after `H` and after `W` respectively. Change the order and the shape\nis the same while the picture is scrambled.",
  "src": "352a702d1ac7"
 },
 "tensor/Tensor.pixelUnshuffle": {
  "en": "The inverse of `pixelShuffle`. Cuts space and stacks it into channels.",
  "src": "4dbad378d800"
 },
 "tensor/Tensor.poisson": {
  "en": "A Poisson sample at each slot's own mean. **A mean of 0 gives 0.**\n\nIt is Knuth's multiplicative form, so **the number of iterations differs per value** — a loop\nthat diverges per slot cannot be moved into a shader, so it reads once here. The same place\nas `histc` and `mode`.",
  "src": "115624bc1123"
 },
 "tensor/Tensor.poissonNllLoss": {
  "en": "The Poisson negative log-likelihood.\n\n**Stirling's correction is added only where `target > 1`** — added unconditionally it is wrong\nonly where the target is small (confirmed by measurement).",
  "src": "8ad41207b6d7"
 },
 "tensor/Tensor.polar": {
  "en": "Builds from magnitude and angle. Where `torch.polar` goes.",
  "src": "ca86fa61a282"
 },
 "tensor/Tensor.poolND": {
  "en": "Pooling independent of dimensionality.",
  "src": "a66c007e9f2f"
 },
 "tensor/Tensor.powScalar": {
  "en": "A constant exponent.\n\n**Integer exponents go through multiplication.** WGSL's `pow(x, y)` is `exp2(y·log2(x))`, so\na negative base has no answer, and what actually comes out looks like `|x|` was used. At even\nexponents the value happens to be right, so `method::pow` passed and it was caught at\n`grad::pow2` with the sign flipped — the kind where the value is right and only the gradient\nis wrong.",
  "src": "077531bd9edd"
 },
 "tensor/Tensor.prelu": {
  "en": "The negative-side slope is **learned.**\n\n**Exactly zero belongs to the negative side.** The forward is 0 either way so nothing shows,\nwhile the gradient diverges — torch gives 1 only where `x > 0`. Splitting on `x < 0` in the\ncore produced a maximum difference of 3.75 at that single point.\n\nThe weight is learned, so it is wired as a binary operation — baked as a constant, no gradient\nflows.",
  "src": "3fcaeb1bc452"
 },
 "tensor/Tensor.prod": {
  "en": "Multiplies everything.",
  "src": "3dc0bf6bf3ee"
 },
 "tensor/Tensor.put": {
  "en": "**Flattens** and writes by index — there is no notion of an axis. The opposite of `take`.\n\nThe indices arrive as values, so it uses the same door as a CPU-built index table.",
  "src": "41c7232384a5"
 },
 "tensor/Tensor.qr": {
  "en": "QR factorisation. **`reduced` has a gradient.**\n\n    N = Qᵀ·Q̄ − R̄·Rᵀ\n    Ā = [Q̄ + Q·(tril(N − Nᵀ, −1) − N)]·R⁻ᵀ\n\nKeeping only the lower triangle is the whole of this derivation — differentiating `QᵀQ = I` makes\n`Qᵀ·dQ` antisymmetric, and the upper half is the lower half's mirror, so there is nothing separate\nto count.\n\n`complete` has leftover columns in `Q` and a different derivation. That side returns values only.",
  "src": "6c2d785538ae"
 },
 "tensor/Tensor.quantile": {
  "en": "Quantiles. **Linear interpolation** — that is torch's default, and it differs from picking the\nnearest value.",
  "src": "a784fa30c3d4"
 },
 "tensor/Tensor.rand": {
  "en": "Uniform on `[0, 1)`. Where `torch.rand` goes, and **the door a user should use.**\n\n**Made on the host and uploaded in one go.** That is what puts it on the same stream as\n`randn`, `randint` and `randperm`, so one `manualSeed` rewinds all four.\n\nThere is one more door randomness comes out of — `Tensor.uniform` is a GPU kernel and uses\nthe dropout counter. That one belongs to places drawing **a sample the size of the\nactivations every step** (dropout, `rrelu`, gumbel) and exists separately to avoid the\nupload. `manualSeed` rewinds both, but **they are not the same stream.**",
  "src": "3c86feec7814"
 },
 "tensor/Tensor.randLike": {
  "en": "`torch.rand_like`. It borrows the shape only — neither the values nor the type are inherited.",
  "src": "ab838416aeda"
 },
 "tensor/Tensor.randint": {
  "en": "Integers in `[low, high)`. **The upper end is excluded**, as in torch.\n\nThe type comes back as `int64` while the values sit in a float32 buffer (`dtype.ts`).\nIntegers above 2^24 cannot be counted there already, so it stops at that point — better\nthan rounding quietly.",
  "src": "456b7b8f3c9b"
 },
 "tensor/Tensor.randn": {
  "en": "The standard normal. Where `torch.randn` goes.",
  "src": "1ac00886ec5a"
 },
 "tensor/Tensor.randnLike": {
  "en": "`torch.randn_like`.",
  "src": "7444fe9934e5"
 },
 "tensor/Tensor.random_": {
  "en": "Fills with integers from `[from, to)`.\n\n**Left out, the bound is as far as f32 counts exactly** (2^24). torch's depends on the dtype and is 2^62 for int64, but int64 here lives in an f32 cell and cannot count past that — drawn values above it have neighbours that cannot be told apart. That is not imitated.",
  "src": "5efd6dde7c5e"
 },
 "tensor/Tensor.randperm": {
  "en": "`0..n-1` shuffled. Where `torch.randperm` goes. Fisher–Yates.",
  "src": "f9b32e281141"
 },
 "tensor/Tensor.range": {
  "en": "The old name for `arange`, except that it **includes the end** (measured). So it has one\nelement more.\n\ntorch marked it deprecated over this same difference. Quietly forwarding to `arange` loses\nthe last value, and that one slot is invisible if you only measure sums or means.",
  "src": "3a5820502e4a"
 },
 "tensor/Tensor.raw": {
  "en": "The buffer holding the values — **without the complex check.** Only code that knows complex uses it.\n\nThe device check still happens. The difference between the two getters is one line about\ncomplex, and that one line is the declaration \"this code knows the interleaved layout\".",
  "src": "6aa517c7a58d"
 },
 "tensor/Tensor.real": {
  "en": "The real part. **Gradient returns as `g + 0i`.**\n\nIn torch this is a strided view; here it is **a copy** — our tensors carry only a shape and no\nstrides, so there is no way to build a frame that reads every other slot. The values are the same.",
  "src": "dbb2d3119f79"
 },
 "tensor/Tensor.remainder": {
  "en": "The remainder — **with a different sign from `fmod`.**\n\ntorch's `remainder` (and Python's and torch's `%`) follows the sign of **the divisor** while\n`fmod` follows the dividend. The remainder of `-7` divided by `3` is 2 on this side and -1 on\nthat. Both being called \"the remainder\" is the trap, and JavaScript's `%` is the `fmod` one, so\nusing it directly diverges quietly on negative inputs only.\n\nThe whole difference is truncation (`trunc`) versus flooring (`floor`).",
  "src": "ce70b374ebbf"
 },
 "tensor/Tensor.renorm": {
  "en": "Pulls **each slice along `dim` down below `maxnorm`** in norm.\n\nLeft as an assembly — **the scale has `x` inside it**, so the backward is not `g·s`. Written by\nhand, the forward is right and the gradient quietly wrong (which is how it went wrong once in the\ncore).",
  "src": "9c7cf676ee2f"
 },
 "tensor/Tensor.repeat": {
  "en": "Repeats an integer number of times per axis. Unlike `expand`, it really does become several copies.",
  "src": "487eaf6227fb"
 },
 "tensor/Tensor.repeatInterleave": {
  "en": "Repeats per slot. `[a,b]` twice each gives `[a,a,b,b]` — different from `tile`.",
  "src": "2fb800cc2726"
 },
 "tensor/Tensor.repr": {
  "en": "The text `print(t)` prints. **It reads back from the GPU, so it is asynchronous.**\n\nTextbooks print this text verbatim, so this project treats it as a specification too — the reasoning\nis in `src/repr.ts`.",
  "src": "8ffefa36b53f"
 },
 "tensor/Tensor.reshape": {
  "en": "Views the same buffer as a different shape. The element order does not change, so no kernel is needed.\n\n**`-1` means \"the rest\".** It is the form torch code always uses — `x.reshape(-1)`,\n`x.view(n, -1)` — and it was missing here: the size-matching check caught it and it stopped\nwith `shape '[-1]' is invalid`. It can be used in one position only, and must divide the\nrest evenly.",
  "src": "5520a6724a94"
 },
 "tensor/Tensor.resolveConj": {
  "en": "A deferred conjugate made real. **Here it is itself** — nothing was deferred.\n\nIn torch this repays the bit `conj()` raised. Our `conj` arrives already repaid, so there is nothing\nto repay and this is the identity. Being the identity and **being absent are different** — putting\nthis line in before handing a conjugate on is a common idiom in torch code.",
  "src": "f894dd16de33"
 },
 "tensor/Tensor.resolveNeg": {
  "en": "A deferred negation made real. The identity, for the same reason as `resolveConj`.",
  "src": "a80e8d951417"
 },
 "tensor/Tensor.rmsNorm": {
  "en": "**It does not subtract the mean.** That is the only difference from `layerNorm`.\n\nThe default eps is f32's machine epsilon rather than `1e-5` — as in torch. Written as `1e-5` to\nmatch the other normalisation layers, the forward stayed inside tolerance and **only the\ngradient** diverged, at a maximum difference of 2.26e-02. It is amplified where the variance is\nsmall.",
  "src": "66819bbdf0e2"
 },
 "tensor/Tensor.roll": {
  "en": "Shifts one axis. **What falls off the end comes back at the front.**\n\nIt is `out[i] = in[(i - shift) mod n]`, so it is the rule table's `mod` with a shift laid on\ntop.",
  "src": "d579acba89c2"
 },
 "tensor/Tensor.rot90": {
  "en": "Rotates by 90° within a two-dimensional plane.\n\nAt `k=1` it is `out[i][j] = in[j][C-1-i]` — swapping the axes while reversing one, which\nwrites down directly as a rule table.",
  "src": "43564b8e9a13"
 },
 "tensor/Tensor.rrelu": {
  "en": "Draws the negative-side slope and uses it.\n\n**In eval mode it settles at the middle** — with the defaults, `(1/8 + 1/3)/2 = 0.2292`.\nRandomness enters in training mode only.",
  "src": "a293b4d9144b"
 },
 "tensor/Tensor.rsub": {
  "en": "`other - alpha * this`. The place where subtraction's sides are swapped.",
  "src": "fb298aa26930"
 },
 "tensor/Tensor.scalarTensor": {
  "en": "A tensor of one number. The same as `full([], v)`, given its own name by torch.",
  "src": "a7dc8515a636"
 },
 "tensor/Tensor.scatterAdd": {
  "en": "**Adds** at the positions the indices point to. Overlaps accumulate.\n\nThe opposite of `gather`. With only one of them you can take out and not put back, and code hand-\nbuilding embeddings or one-hots meets that immediately.",
  "src": "f1a910359b40"
 },
 "tensor/Tensor.scatterReduce": {
  "en": "The same place as `scatter`, but **combining instead of overwriting.**",
  "src": "2a7054a159f9"
 },
 "tensor/Tensor.scatterSet": {
  "en": "**Overwrites** at the positions the indices point to. On an overlap, the last write stands.",
  "src": "cb0afe5a7d6e"
 },
 "tensor/Tensor.scatterSpots": {
  "en": "A **copy** with `src` written at the index table's positions.",
  "src": "d86d2b78fcbd"
 },
 "tensor/Tensor.searchSorted": {
  "en": "Where each of `values` would go inside the **sorted** boundaries (`this`).\n\nIt is `torch.searchsorted(sorted_sequence, values)`, and since the receiver is the\nboundaries, here it is a method on the boundaries. The answer is `int64` and its shape\nfollows `values`.\n\n**Which way a tie goes is half of this function.** The default is left — where the same\nvalue already exists, it stands **before** it (the count of things smaller than me). Given\n`right` it stands after. The Python side takes this under **two names**, `right` (boolean)\nand `side` (string), and reconciling those two happens there — this side knows only one.\n\n**It does not check that the input is sorted.** torch does not either (checking costs another\nO(n) pass, and anywhere you would pay that you would not be using this function). Given\nsomething unsorted the answer does not quietly lose meaning — the binary search simply\nproduces some position.",
  "src": "27506d3bc0af"
 },
 "tensor/Tensor.select": {
  "en": "Picks one position along an axis and removes that axis.",
  "src": "5b366366f23a"
 },
 "tensor/Tensor.selectScatter": {
  "en": "A **copy** with the plate `select` would have taken swapped out.",
  "src": "c9c5fa0642a9"
 },
 "tensor/Tensor.shape": {
  "en": "**Not `readonly`** — one place, `mutate`, edits it.\n\nThere are in-place operations that change the shape (`transpose_`, `squeeze_`,\n`unsqueeze_`). Those edit the frame you view through rather than the values, so the slot\nstays and only the shape is swapped. Nowhere but that one place changes it.",
  "src": "b615aed4a6cf"
 },
 "tensor/Tensor.size": {
  "en": "**Not `readonly`** — the same reason as `shape`, and the same one place edits it.\n\n`as_strided_` changes even the slot count. Other in-place operations only change the frame,\nso \"the slot count does not change\" was true for a long time, and then a name appeared for\nwhich it is not.",
  "src": "1eae1dc48b29"
 },
 "tensor/Tensor.sizeRepr": {
  "en": "`torch.Size([2, 2])`. The shape is printed too, so it is a specification.",
  "src": "c33576402407"
 },
 "tensor/Tensor.sliceScatter": {
  "en": "A **copy** with the `x[..., start:end:step]` region swapped out.",
  "src": "8425bc3e01a1"
 },
 "tensor/Tensor.slogdet": {
  "en": "The sign and the log absolute value, separately. No digits are lost even when the determinant is very small.",
  "src": "a16602a4d22f"
 },
 "tensor/Tensor.softMarginLoss": {
  "en": "`log(1 + e^{−y·x})`. Going through `softplus` keeps it from overflowing at large values.",
  "src": "ea65ce59b4e0"
 },
 "tensor/Tensor.softmax": {
  "en": "`exp(x) / Σ exp(x)`. **Computed with the maximum subtracted** — otherwise it overflows at large values.",
  "src": "7b81e78a6034"
 },
 "tensor/Tensor.softmin": {
  "en": "softmax(−x). **Miss the sign and it becomes softmax** — only the values separate them.",
  "src": "acfcdc81ee46"
 },
 "tensor/Tensor.softplus": {
  "en": "(1/β)·log(1+e^{βx}). **Past the threshold, βx is simply x.**\n\nLeave that branch out and `exp` overflows on large inputs, and every gradient after it is\nNaN.",
  "src": "e07bab6d8af6"
 },
 "tensor/Tensor.softshrink": {
  "en": "Pulls **towards the origin** by λ. Unlike `hardshrink` the values stay continuous.",
  "src": "4bb56868172c"
 },
 "tensor/Tensor.solve": {
  "en": "`A x = b`. The backward is `A⁻ᵀ·Ḡ` towards `b` and `-A⁻ᵀ·Ḡ·xᵀ` towards `A`.\n\nWhen `b` has one axis fewer than `A` it is read as **a bundle of vectors** — torch's rule. The\nbackward's outer product hangs on that distinction.",
  "src": "0c37bb2ad1d8"
 },
 "tensor/Tensor.solveEx": {
  "en": "The non-throwing side of `solve`.",
  "src": "18404d7f4005"
 },
 "tensor/Tensor.solveTriangular": {
  "en": "Solves knowing the matrix is triangular.",
  "src": "9bbc3fd5695d"
 },
 "tensor/Tensor.sort": {
  "en": "Sorts one axis and produces the values and the positions.\n\n**The gradient follows the values.** It flows only to the positions taken and is zero elsewhere,\nand returning the values detached means no gradient reaches those positions and the whole\nclassification loss becomes non-differentiable. The core went through this with `topk` and `sort`,\nand the sister project was in the same state until review.",
  "src": "2a5c8eea2738"
 },
 "tensor/Tensor.split": {
  "en": "Divides one axis into equally sized pieces. Each piece is a new tensor.",
  "src": "18a53110c49c"
 },
 "tensor/Tensor.splitSize": {
  "en": "Divides one axis by size. `split` takes the piece **size**, `chunk` the piece **count**.",
  "src": "e0090c28f832"
 },
 "tensor/Tensor.splitWithSizes": {
  "en": "Splits by **a list of piece sizes.** `tensorSplit` takes the **positions** to cut at.",
  "src": "2e926d7dbdf1"
 },
 "tensor/Tensor.squeeze": {
  "en": "Removes an axis of size 1.",
  "src": "a1caa134ea60"
 },
 "tensor/Tensor.squeezeAll": {
  "en": "Removes **every** axis of size 1. The argument-free `squeeze()`.",
  "src": "dcb2463b5d42"
 },
 "tensor/Tensor.stack": {
  "en": "Stacks by making a new axis. The same as `cat` with an axis inserted.",
  "src": "e42e089b5ea5"
 },
 "tensor/Tensor.stdMean": {
  "en": "The standard deviation and mean together. One name for the same reason as `aminmax`.",
  "src": "eba1321b02ba"
 },
 "tensor/Tensor.sum": {
  "en": "Adds everything. Given `dtype` it converts **before** adding.\n\n**Every neighbour took that argument and only this one did not.** `mean`, `prod` and `nansum`\nare `(dim?, keepdim, dtype?)`, and `cumsum` and `cumprod` take it too. `sumDim`, the\naxis-taking side, takes it. **The single most-called name among the reductions** was the one\nmissing it, so there was nowhere to carry `torch.sum(x, dtype=torch.float32)` across to.\n\nOne of four not listening is worse than none of them listening — because no rule can be\nformed. `norm` had already shown this same place once.\n\n**The order decides the value.** Convert first, then add — folding `[1.7, −2.3, 0.9]` to\nint64 gives −1 truncating first and 0 truncating last. torch does the former.",
  "src": "a482f2103323"
 },
 "tensor/Tensor.svd": {
  "en": "Singular value decomposition.\n\n**The singular values have a gradient and `U` and `Vh` do not.** Since `dS = diag(Uᵀ·dA·V)`, the\nsingular-value side is the single line `Ā = U·diag(Ḡ)·Vᵀ` with no degeneracy problem. The vector\nside brings in `1/(sᵢ²−sⱼ²)` and blows up when singular values coincide, and that part was left\nout.\n\n`fullMatrices` (true by default) fills `U` out to `rows×rows` — torch's default. The direction of\nthe fill is not unique once more than one dimension is left over (see `completeBasis`).\n**What the backward uses is the reduced form, before the fill.**",
  "src": "96c866b17f4b"
 },
 "tensor/Tensor.svdLowrank": {
  "en": "**A low-rank SVD** obtained by random projection. `(U, S, V)`, and **V is not transposed.**\n\n**The answer is stable only on input that is exactly low-rank.** torch projects with a random\nmatrix, and once the rank exceeds `q` the singular values move by around 0.5 depending on the seed\n(measured). At rank `q` or below, changing the seed stays within 7e-7 — that is the only place\nworth asking.\n\nWe do not project. The full SVD is computed and the first `q` taken — the same answer where the\ninput is exactly low-rank, and **more accurate than torch** where it is not.",
  "src": "d6bd70c8afb3"
 },
 "tensor/Tensor.svdvals": {
  "en": "The singular values only. The middle of `svd`.",
  "src": "8834b1549121"
 },
 "tensor/Tensor.swapaxes": {
  "en": "Swaps two axes. The same as `swapdims` — torch carries both names.",
  "src": "5385bcde74d2"
 },
 "tensor/Tensor.tensorInv": {
  "en": "Folds the tensor into a matrix, inverts, and returns the axis order.",
  "src": "0b3f1f166ebe"
 },
 "tensor/Tensor.tensorSolve": {
  "en": "Folds the tensor into a matrix, solves, and spreads it back.",
  "src": "5a41972d3649"
 },
 "tensor/Tensor.tensorSplit": {
  "en": "**The remainder is shared out from the front.** Splitting 10 into 4 gives 3·3·2·2 (measured).\n\nDifferent from `chunk` — that one fills the front pieces fully and the last takes what is\nleft. Measured only at sizes that divide evenly, the two functions look the same.",
  "src": "31b556b6481d"
 },
 "tensor/Tensor.tensordot": {
  "en": "Folds and multiplies along the named axes. It herds the folded axes and finishes with **one matrix multiply.**",
  "src": "a9a84372d354"
 },
 "tensor/Tensor.threshold": {
  "en": "x > t passes through, otherwise `value`. **The boundary goes to the value side.**",
  "src": "a44c854c43fb"
 },
 "tensor/Tensor.tile": {
  "en": "The same work as `repeat`, but torch carries both names.",
  "src": "aa083c64db17"
 },
 "tensor/Tensor.toArray": {
  "en": "The values as a flat f32 array.\n\n**A complex comes out interleaved as it is stored** — the length is `2 × size` rather than `size`, and\nit reads `[re, im, re, im, …]`. For the real part alone call `real()` first; for pairs, `viewAsReal()`.\nPicking out the real part here would make the length plausible and hide that values were lost.",
  "src": "f3362ed86c2f"
 },
 "tensor/Tensor.topk": {
  "en": "The largest `k`. The front of `sort` — torch gives them descending too.",
  "src": "b25b92afb910"
 },
 "tensor/Tensor.trace": {
  "en": "The sum of the diagonal.",
  "src": "fc523ec41527"
 },
 "tensor/Tensor.transpose": {
  "en": "Two-dimensional transpose. For now it actually moves the data — views are T1.",
  "src": "371cb2735186"
 },
 "tensor/Tensor.trapezoid": {
  "en": "Adds **the mean of each neighbouring pair times the spacing.** Trapezoidal integration.\n\nIt was absent here for a long time. The binding was assembling the same few lines in Python, and\nthe comment said \"making one more here would be a second copy of the assembly\". What that missed\nis that **the name did not exist at all for anyone using borch.ts from TypeScript** — the\nassembly was not one copy; it was only on the Python side.\n\nIt is only slicing and adding, so **the gradient flows on its own.** There is nothing to write\nby hand.",
  "src": "be94d9f64a0f"
 },
 "tensor/Tensor.triangularSolve": {
  "en": "**It gives two** — the solution, and a **copy** of the coefficient matrix handed in (measured).\n\nThe same computation as `solveTriangular` with the argument order flipped and **`upper` defaulting\nto true.** Miss those two and you solve a different triangle with a plausible-looking value.",
  "src": "652608e5114f"
 },
 "tensor/Tensor.tril": {
  "en": "The lower triangle. Zeroes everything above `diagonal`.",
  "src": "710633b6c352"
 },
 "tensor/Tensor.tripletMarginLoss": {
  "en": "`max(0, d(a,p) − d(a,n) + margin)`.",
  "src": "558bc2f4eac0"
 },
 "tensor/Tensor.typeAs": {
  "en": "To the same type as `other`. torch's `type_as`.",
  "src": "dd89d8275316"
 },
 "tensor/Tensor.unbind": {
  "en": "Tears an axis apart into singles. That axis disappears.",
  "src": "161077ed8ea6"
 },
 "tensor/Tensor.unflatten": {
  "en": "Spreads one axis into several.",
  "src": "f46ac4844b98"
 },
 "tensor/Tensor.unfold": {
  "en": "A sliding window. With a step smaller than the window, the windows overlap.\n\n**Overlaps accumulate in the backward** — spreading a length of 5 with `unfold(0, 3, 1)`\ngives a gradient of `[1,2,3,2,1]`. Without the addition they would all be 1, and a value\ncheck alone does not catch it.",
  "src": "63fcfb04271c"
 },
 "tensor/Tensor.unfoldIm2col": {
  "en": "Spreads windows into columns. `(N, C, H, W)` → `(N, C·kh·kw, L)`.",
  "src": "41d9f1ef2d2b"
 },
 "tensor/Tensor.uniform": {
  "en": "Random numbers between 0 and 1. `rrelu` and `gumbelSoftmax` use it.\n\nIt uses the seed `manualSeed` catches — the same seed has to give the same draw for anything\nto reproduce.",
  "src": "b0bce261289d"
 },
 "tensor/Tensor.unique": {
  "en": "The distinct values, **ascending.** torch's default is to give them sorted.",
  "src": "b96c09ece2dc"
 },
 "tensor/Tensor.uniqueConsecutive": {
  "en": "Collapses **consecutive** duplicates only. Unlike `unique` it does not sort — `[1,1,2,2,1]`\nbecomes `[1,2,1]` (measured). Measured with sorted input alone, the two look the same.\n\nThe result's **length depends on the values**, so it reads once here. The same place as `unique`\nand `bincount`, which is why those three alone are asynchronous.",
  "src": "80e5b664cbe7"
 },
 "tensor/Tensor.unravelIndex": {
  "en": "Unpacks a flat index into per-axis indices. **One tensor per axis, returned as a bundle** (measured).\n\nThe arithmetic is division and remainder only, so no values are read.",
  "src": "20d8b9c43832"
 },
 "tensor/Tensor.unsqueeze": {
  "en": "Inserts an axis of size 1.",
  "src": "a339317d470e"
 },
 "tensor/Tensor.upsample": {
  "en": "Nearest-neighbour enlargement. `Upsample` and `interpolate` are this.",
  "src": "4078f1b0f0f0"
 },
 "tensor/Tensor.vander": {
  "en": "The Vandermonde matrix. **The default is decreasing powers** — the last column is 1 (measured).\n\nThe powers are not written with `pow`. WGSL's `pow` is `exp2(y·log2(x))`, so **a negative\nbase has no answer**, and negative inputs are ordinary for a Vandermonde. Counting with a\nrunning product is exact.",
  "src": "54b51fe0b60b"
 },
 "tensor/Tensor.variance": {
  "en": "Variance. **torch's default is the unbiased estimate (dividing by n-1)** — left at\n`correction=0` the value comes out subtly smaller, and that becomes the place it diverges\nquietly inside a normalisation layer.",
  "src": "84d8c3dc4d10"
 },
 "tensor/Tensor.vecdot": {
  "en": "The inner product, treating the last axis as a vector.",
  "src": "698b0bb77af6"
 },
 "tensor/Tensor.vectorNorm": {
  "en": "The norm measured treating it as a vector. **Given a matrix it flattens the whole thing** — where\nit parts from `matrixNorm`.\n\n`ord=0` is the count of non-zeros, and `±Infinity` the maximum and minimum absolute value.\nBranches that must not go through the power formula, so they are written separately.",
  "src": "3037c60d45ad"
 },
 "tensor/Tensor.view": {
  "en": "Views the same buffer as a different shape. The same as `reshape`, and in-place operations carry across.",
  "src": "be3653f6c81f"
 },
 "tensor/Tensor.viewAsComplex": {
  "en": "Views a pair of reals as a complex. The opposite of `viewAsReal`, and a view likewise.",
  "src": "eaa20f9562ea"
 },
 "tensor/Tensor.viewAsReal": {
  "en": "**Views a complex as a pair of reals** — a 2 is appended to the shape. The buffer is not moved.\n\nIt would be fair to say the interleaved storage exists for this one line. It is a view in torch\ntoo.",
  "src": "97207b36c591"
 },
 "tensor/Tensor.webgpu": {
  "en": "Puts the values up on the GPU. Where torch's `t.cuda()` goes.\n\n**Unlike `cpu()` it is synchronous.** Uploading is one queued write and there is nothing to wait for\n— it looks asymmetric, and that is better than inventing a round trip to dress up the symmetry.",
  "src": "20b1412984b3"
 },
 "tensor/Tensor.where": {
  "en": "This side or that, per position of the condition. torch's method form is `x.where(cond, other)`.",
  "src": "0ccdee8ee9bb"
 },
 "tensor/alignStrides": {
  "en": "The strides when `shape` is placed right-aligned against `out`'s rank.\n\n**A stretched axis is 0** — it reads the same value over and over. Actually replicating to\nstretch costs memory, and that is exactly why im2col lost to the fused kernel in the conv\nbenchmark.",
  "src": "d77e19c41dbb"
 },
 "tensor/broadcastShapes": {
  "en": "torch's broadcasting rule. Aligned from the right, 1 stretches, and the rest must match.",
  "src": "8ae498476004"
 },
 "tensor/currentDevice": {
  "en": "The device currently attached, or `null` if none. Where `torch.accelerator` goes.\n\n**Unlike `dev()` it does not throw** — its purpose is to ask whether one is attached, and\nthat question itself must not fail.",
  "src": "7e8ecd9b4b69"
 },
 "tensor/device": {
  "en": "Where released buffers are counted. Benchmarks watch leaks here.",
  "src": "ab7a1c85935f"
 },
 "tensor/init": {
  "en": "Acquires the WebGPU adapter. From the second call on it returns the one already acquired.\n\n**`options` is heard on the first call only.** There is one device, and once it is made\nthere is nothing left to choose — better written here than quietly ignored.",
  "src": "5b9d94ed599c"
 },
 "tensor/keepAlive": {
  "en": "Keeps something alive past a scope's close. Parameters and optimizer state use it.",
  "src": "b95da887e3e1"
 },
 "tensor/makeNode": {
  "en": "Makes one graph node from outside. **A door for places writing kernels in another file.**\n\n`Tensor.make` is private — it is what this class's own operations use, and that is right. But once a\nmodule with **kernels of its own** appears, such as `fft.ts`, that door is needed. Rather than\nrestating the conditions there (two copies of the `gradMode` check and the `requiresGrad` propagation\neventually diverge), it hands over to the same place.",
  "src": "d46b697b889c"
 },
 "tensor/scope": {
  "en": "Opens and closes one scope. GPU buffers made inside are released on the way out.\n\n**A training loop does not run without this.** One step makes thousands of intermediate buffers, and\nJavaScript's garbage collector does not return GPU memory in time.\n\n## Two spellings — the same machine\n\n```ts\nawait scope(async () => {           // callback\n  ...\n  return await loss.item();\n});\n\n{\n  using s = scope();                // block. closer to torch's `with`\n  ...\n  await loss.item();\n}                                   // closes here\n```\n\n**The block form closes after the `await`s.** Inside an async function, the block is left only after\nevery `await` in it has finished, and releasing (`endScope`) is itself synchronous, so `using` works\nrather than `await using` — and that one is more widely supported.\n\n**The callback form is not removed.** Both stand on the same `beginScope`/`endScope`, so there is\nnowhere for them to diverge, and where you want the value handed straight back\n(`const loss = await scope(...)`) the callback is shorter.",
  "src": "c5b1d71bdb26"
 },
 "vision/": {
  "en": "Transforms shaped like `torchvision.transforms`.\n\n## Why this is inside a tensor library\n\nRunning training requires a place where images become tensors, and there are things\nthat go quietly wrong at that place — the comment on `ToTensor` below is one of them.\nIt uses **the same rules** as the repository's Python side (`borch_vision.py`). If\nthe rules diverge, the same data becomes different values per library, and that is\ncaught only by comparing values.\n\n## An image is an array, not a tensor\n\n`RandomHorizontalFlip` and `RandomCrop` take an `(H, W, C)` array. What arrives here\nin torchvision is a PIL image, and having no PIL, an array stands in its place.\nMaking a tensor per image makes one GPU buffer per image, which looks like it works\nright up until it collapses on memory.",
  "src": "72d220285982"
 },
 "vision/Compose": {
  "en": "Transforms in sequence. `repr` is several lines with the inside indented, so that shape is matched too.",
  "src": "e5bf15bb0d54"
 },
 "vision/Image": {
  "en": "An image laid out as `(H, W, C)`. The values are uint8 or float.",
  "src": "c2e33f58bbcd"
 },
 "vision/Image.isByte": {
  "en": "If uint8, `ToTensor` divides by 255.",
  "src": "2e8d01124f0e"
 },
 "vision/Normalize": {
  "en": "`(x - mean) / std`, per channel.",
  "src": "9cb730d16504"
 },
 "vision/RandomCrop": {
  "en": "Pads the edges, then crops at random.",
  "src": "5380f8f52af7"
 },
 "vision/RandomHorizontalFlip": {
  "en": "Flips left to right. Takes an `(H, W, C)` array.",
  "src": "c9ead36eca40"
 },
 "vision/ToTensor": {
  "en": "`(H,W,C)` or `(H,W)` → a `(C,H,W)` tensor.\n\n**It divides by 255 only when the input is uint8** — that is torchvision's rule. Pass\na float array and it is carried over undivided. Miss this distinction and data that\nis already in [0,1] gets divided once more and comes out 255× darker, with **no\nexception raised and only the training failing.**",
  "src": "ae4de2e997fb"
 },
 "vision/augmentBatch": {
  "en": "Augments an `(N,C,H,W)` batch in one pass. **Ours, not torchvision's.**\n\nMaking a tensor per image makes one GPU buffer per image — ten thousand of them for a\nten-thousand-image epoch. Augmenting the whole batch on the CPU and then making\n**one** tensor is the only order that holds, so that order is given a name and\nexported.\n\n**The random draws are per image.** Using one crop and one flip across the whole\nbatch means nothing inside the batch was augmented relative to anything else, and the\npoint of augmentation is gone.",
  "src": "a5e0ce6add28"
 },
 "vision/normalizeBatch": {
  "en": "`(x - mean) / std` per channel, batch-wide on the CPU — finished before any tensor is made.",
  "src": "1260ebbe148f"
 }
}
